//! Quick tips pool for FlashAgent TUI.
//! Contains 2,000 concise, practical, non-repeating developer tips randomly displayed in the UI.

/// Static pool of 2,000 developer tips across shortcuts, workflows, tools, git, Rust, Linux, and architecture.
pub static TIPS_POOL: &[&str] = &[
    "Press Tab with an empty prompt to open the Settings menu.",
    "Press F2 to cycle Verbose mode: off, last turn, or all reasoning and tool cards.",
    "Press F3 to open the interactive, searchable model selection menu.",
    "Press F4 to adjust thinking effort between off, low, medium, and high.",
    "Press Shift+Tab to quickly cycle through permission modes.",
    "Press Ctrl+O to expand only the last turn of reasoning and tool calls.",
    "Press Alt+O to expand all reasoning and tool call cards across the session.",
    "Press Esc while the model is responding to interrupt execution immediately.",
    "Press Esc on an empty input prompt to exit FlashAgent cleanly.",
    "Use PageUp and PageDown to scroll through chat history and long menus.",
    "Mouse wheel and trackpad scrolling are fully supported in chat view.",
    "Type / at the start of a prompt to open the command autocomplete menu.",
    "Use Up and Down arrow keys at an empty prompt to browse prompt history.",
    "Type /context to view a detailed token breakdown of active context.",
    "Type /settings to adjust backend endpoint, temperature, and options.",
    "Type /model to browse and switch between LLM models dynamically.",
    "Type /verbose all to keep all reasoning blocks and tool cards expanded.",
    "Type /verbose last to expand only the most recent assistant response.",
    "Type /verbose off to keep reasoning and tool details collapsed.",
    "Type /clear to reset current conversation scrollback and start fresh.",
    "Type /help to see all available keyboard shortcuts and slash commands.",
    "Type /exit or /quit to terminate the application immediately.",
    "Double-press Esc in search inputs to quickly clear your search query.",
    "Use Tab to auto-complete commands and file paths in the autocomplete popup.",
    "Hold Shift and scroll with trackpad or mouse wheel for accelerated scrolling.",
    "Terminal window resizes automatically reflow text and adjust card widths.",
    "Use Enter to select highlighted items in menus, and Esc to cancel.",
    "In multi-line responses, code blocks are syntax highlighted automatically.",
    "Tool calls in flight show an animated braille spinner and elapsed time.",
    "Diff previews display added lines in green and removed lines in red.",
    "The bottom bar shows real-time token usage and context percentage.",
    "Context bar turns amber at 75% and red at 90% capacity.",
    "Press Ctrl+C to cancel current text input without exiting the program.",
    "Terminal bracketed paste mode is supported for pasting large snippets.",
    "ANSI colors adapt automatically to both dark and light terminal palettes.",
    "Use /undo to revert the last agent file modifications using git history.",
    "Type /diff to inspect unstaged git modifications made by FlashAgent.",
    "You can customize backend URLs directly in the Settings menu.",
    "Model lists can be filtered in real-time by typing any substring.",
    "Press Enter on the Active Model row in Settings to open the model picker.",
    "Press Enter on the Backend URL row in Settings to launch the Setup Wizard.",
    "Press Enter on Thinking Effort in Settings to choose reasoning presets.",
    "The Setup Wizard can be relaunched anytime using the --setup CLI flag.",
    "Run flashagent-tui --help from the shell to inspect all startup flags.",
    "Pass --yes on startup to skip the directory trust confirmation prompt.",
    "Pass --model <name> on startup to override the active model immediately.",
    "Pass --url <endpoint> on startup to connect to a specific inference server.",
    "In setup wizard, Esc on any input field clears current text first.",
    "In setup wizard, pressing Esc on step 1 safely aborts setup and exits.",
    "Setup wizard verifies local tool execution before completing onboarding.",
    "Type /goal <task> to launch fully autonomous execution with no interruptions.",
    "In /goal mode, FlashAgent runs until the task is complete without asking questions.",
    "When a /goal finishes, your previous permission mode and limits are restored.",
    "Use /goal for multi-file refactoring tasks, test suites, or large migrations.",
    "Interrupt an autonomous /goal at any time by pressing Esc.",
    "Autonomous /goal automatically drafts structured verification steps.",
    "In /goal mode, the model utilizes maximum reasoning effort for deep thought.",
    "Use /goal to investigate complex bugs and formulate verified fixes.",
    "A mission framing card displays active goal parameters and step budget.",
    "Combine /goal with a clear definition of done in your prompt for best results.",
    "Autonomous goals enforce safety rules and sandbox protections automatically.",
    "You can track goal progress via real-time tool execution notifications.",
    "Goals can create new files, edit code, and run test suites autonomously.",
    "If a goal encounters a compile error, it reads the error and fixes it.",
    "Goals automatically execute linter checks before finishing their work.",
    "Use /goal to automate repetitive boilerplate creation across projects.",
    "FlashAgent commits milestone progress to git during large autonomous goals.",
    "Autonomous goals maintain full context awareness across dozens of tool steps.",
    "You can inspect all tool calls made during a goal in Verbose mode.",
    "Goals summarize all completed actions and touched files in a closing report.",
    "Use /goal to generate complete unit test coverage for uncovered modules.",
    "FlashAgent handles broken dependencies autonomously during /goal runs.",
    "Goal execution maintains an internal scratchpad for multi-step planning.",
    "Provide relevant file paths in /goal to narrow the search scope immediately.",
    "Autonomous mode respects session tool blacklists to prevent unwanted actions.",
    "Use /goal to perform automated dependency upgrades and test passes.",
    "Goals run verification commands repeatedly until all checks succeed.",
    "When /goal completes, a restoration banner confirms restored settings.",
    "Use /goal to translate legacy codebases into idiomatic modern Rust.",
    "Goals optimize file edits to minimize diff sizes and avoid conflicts.",
    "In /goal mode, the token budget is expanded to accommodate complex tasks.",
    "You can review every diff produced during a goal in the audit log.",
    "FlashAgent will self-correct if a tool execution returns an error code.",
    "Use /goal to audit security vulnerabilities and apply recommended patches.",
    "Autonomous goals automatically format generated code using cargo fmt.",
    "You can resume or refine a finished goal by following up in chat.",
    "Default mode is Accept Edits: file writes are auto-approved, shell is gated.",
    "In Planning mode, FlashAgent is strictly read-only with no file or shell access.",
    "In Manual mode, FlashAgent asks for confirmation before every write and command.",
    "In Accept All mode, all file modifications and shell commands run without prompts.",
    "Cycle permission modes easily using Shift+Tab at the input prompt.",
    "File write approvals always show a syntax-highlighted diff preview.",
    "Shell command approvals show the exact command line and working directory.",
    "Session allow-rules remember approved shell command prefixes.",
    "Blacklisted tools cannot be invoked under any permission mode.",
    "Permission mode status is always visible inside the composer sub-line.",
    "Switching to Planning mode lets you safely inspect code without risks.",
    "Accept Edits mode strikes the ideal balance between speed and security.",
    "Shell command gating prevents accidental execution of destructive commands.",
    "Diff previews show exact line replacements with context lines included.",
    "You can inspect active permission state anytime via the Settings menu.",
    "Planning mode is great for architectural research and code explanations.",
    "Manual mode gives you granular control over every single file modification.",
    "Accept All mode is recommended only in isolated container or VM environments.",
    "FlashAgent never modifies files outside your project workspace root.",
    "Always review shell commands touching git remote or production endpoints.",
    "Session rules can be viewed and cleared within the application settings.",
    "Accept Edits mode allows rapid iterative coding while keeping shell safe.",
    "You can deny a pending tool call by pressing Esc or selecting Deny.",
    "Approved shell prefixes apply only to the current working session.",
    "You can lock down permissions permanently by configuring config.json.",
    "FlashAgent includes free DuckDuckGo web search without any API keys.",
    "FlashAgent can run shell commands in foreground or spawn background tasks.",
    "Background processes can be monitored and terminated via task manager.",
    "Tool outputs exceeding token budgets are truncated with byte markers.",
    "Multi-turn tool execution streams intermediate reasoning in real time.",
    "Web search queries can be restricted to specific documentation domains.",
    "Background tasks continue executing while you chat with the model.",
    "You can send stdin input to background tasks using the task management tool.",
    "FlashAgent detects git repositories automatically upon opening a directory.",
    "File edits preserve original line endings (LF vs CRLF) automatically.",
    "Diff previews format additions and deletions using standard unified format.",
    "You can ask FlashAgent to search online documentation for new libraries.",
    "FlashAgent can fetch and parse web documentation pages into clean markdown.",
    "Grep searches ignore target/, .git/, and node_modules by default.",
    "Find by name respects .gitignore rules to prevent cluttered search results.",
    "Shell execution supports standard bash syntax, pipelines, and redirects.",
    "FlashAgent detects and displays command exit codes upon completion.",
    "Tool calls are logged in transcript.jsonl for complete session auditing.",
    "Use web_search to look up the latest API documentation and crate releases.",
    "Edit operations validate that target content exists before modifying.",
    "Write operations create parent directory trees if they do not exist.",
    "You can ask FlashAgent to run unit tests and parse failure stack traces.",
    "Run cargo test --workspace to run all crate unit and integration tests.",
    "Run cargo clippy --workspace -- -D warnings to catch code smells and bugs.",
    "Use cargo check instead of cargo build for rapid type checking.",
    "Run cargo test -p <crate> --test <test_name> to focus on a single failing test.",
    "Use dbg!(expr) for temporary inline debug prints in Rust development.",
    "Use div_ceil(2) in Rust 1.73+ instead of manually writing (n + 1) / 2.",
    "Prefer matches!(val, Pattern) over verbose match statements for single checks.",
    "Use saturating_sub on usize variables to prevent underflow panics in TUI.",
    "Use Cow<'a, str> to avoid unnecessary allocations when strings are borrowed.",
    "Leverage RAII and Drop guards for automatic cleanup of terminal state.",
    "Use cargo fmt to ensure consistent code formatting across the repository.",
    "Mark internal functions with pub(crate) to maintain clean public crate APIs.",
    "Use Option::take() when moving values out of mutable references.",
    "Prefer .entry() on std::collections::HashMap for clean insertion logic.",
    "Use let-else statements (let Some(x) = opt else { return; }) for early exits.",
    "Run cargo tree -d to identify and resolve duplicate dependency versions.",
    "Use cargo bench to benchmark performance-critical code paths.",
    "Avoid unwrap() in library code; use Result and custom errors with thiserror.",
    "Use anyhow::Context to attach clear context strings to bubbled errors.",
    "Use tokio::sync::mpsc channels for asynchronous actor message passing.",
    "Use Arc<Mutex<T>> when sharing mutable state across asynchronous tasks.",
    "Prefer RwLock over Mutex when read operations vastly outnumber writes.",
    "Use std::sync::atomic types for lock-free counters and boolean flags.",
    "Use #[inline] judiciously on small, frequently called helper functions.",
    "Run cargo clippy --fix to automatically apply machine-applicable lints.",
    "Use BTreeMap when you need ordered keys or range queries over collections.",
    "Use SmallVec or TinyVec to avoid heap allocations for small collections.",
    "Prefer Iterator adapters (map, filter, fold) over explicit for-loops.",
    "Use str::strip_prefix and strip_suffix instead of slicing string indexes.",
    "Avoid indexing strings directly; use .chars() or .char_indices() for UTF-8.",
    "Use cargo test -- --nocapture to see stdout from tests during development.",
    "Use cargo test -- --ignored to run long-running or external integration tests.",
    "Structure workspace crates logically: core logic, domain types, and UI.",
    "Use feature flags in Cargo.toml to keep release binaries slim and fast.",
    "Run cargo audit to scan dependencies for known security vulnerabilities.",
    "Use cargo outdated to find dependencies with newer semantic versions.",
    "Implement std::fmt::Display for user-facing string formatting of types.",
    "Implement Default trait for structs to make configuration and tests easy.",
    "Use #[derive(Default)] when all struct fields implement Default.",
    "Use serde rename and skip attributes to control serialization behavior.",
    "Prefer static dispatch with generics over dynamic dispatch with dyn Trait.",
    "Use dyn Trait when heterogeneous collections or plugin architectures need it.",
    "Use once_cell or std::sync::OnceLock for thread-safe lazy initialization.",
    "Use mem::replace to swap struct fields without cloning expensive values.",
    "Use Vec::with_capacity when the final size of a vector is known in advance.",
    "Use String::with_capacity when building large strings in loops.",
    "Use path.strip_prefix() to create relative paths for cleaner UI display.",
    "Use std::path::Path::new() instead of strings for portable filesystem paths.",
    "Check std::env::var_os() when reading OS environment variables safely.",
    "Use crossterm::terminal::size() to react to terminal dimensions dynamically.",
    "Use crossterm cursor and event queues for flicker-free terminal drawing.",
    "Restore terminal alternate screen and raw mode in panic hooks reliably.",
    "Catch panics with std::panic::catch_unwind when wrapping foreign code.",
    "Use parking_lot mutexes for faster lock acquisition without OS overhead.",
    "Use tracing and tracing-subscriber for structured, async-aware logging.",
    "Keep critical sections inside mutex locks as short as possible.",
    "Avoid holding mutex guards across .await points to prevent deadlocks.",
    "Use tokio::select! to handle timeouts and cancellation tokens cleanly.",
    "Use futures::stream::StreamExt for processing asynchronous event streams.",
    "Use async_trait crate when defining asynchronous methods in Rust traits.",
    "Implement Clone on lightweight handles that wrap Arc internally.",
    "Use serde_json::to_string_pretty for human-readable JSON diagnostics.",
    "Use Box<dyn Error + Send + Sync> for generic error handling in prototypes.",
    "Use #[must_use] on functions returning values that should not be ignored.",
    "Run cargo clean -p <crate> to recompile a single crate from scratch.",
    "Leverage cargo check --tests to verify unit test compilation rapidly.",
    "Use cargo expand to inspect macro-generated code during debugging.",
    "Avoid unsafe code unless benchmarking proves it provides massive gains.",
    "Document public traits and functions with doc-comments and code examples.",
    "Write unit tests close to the implementation in a nested tests module.",
    "Run git status -s before starting a task to ensure a clean working tree.",
    "FlashAgent generates conventional commit messages matching project style.",
    "Use git diff --staged to review changes before committing.",
    "Create atomic commits for individual features or fixes for easy bisecting.",
    "Use git stash -u to save untracked files when switching test branches.",
    "Use git worktree to work on parallel branches without cloning repeatedly.",
    "Run git log --oneline -n 10 to inspect recent repository commit history.",
    "Use git diff HEAD~1 to verify the changes introduced by the last commit.",
    "Keep branch names descriptive, e.g. fix/ansi-wrapping or feat/goal-mode.",
    "Always verify git status after FlashAgent finishes modifying files.",
    "Use git add -p to stage changes interactively chunk by chunk.",
    "Run git commit --amend --no-edit to touch up the most recent commit cleanly.",
    "Use git clean -nd to dry-run removal of untracked build artifacts.",
    "Use git clean -fd to remove untracked files and directories permanently.",
    "Run git restore <file> to discard uncommitted working tree modifications.",
    "Run git restore --staged <file> to unstage files while preserving edits.",
    "Use git log --graph --decorate --oneline to view branch merge history.",
    "Use git blame -L <start>,<end> <file> to see who modified specific lines.",
    "Run git bisect to find the exact commit that introduced a regression.",
    "Use git cherry-pick <commit> to apply a specific commit to your branch.",
    "Keep .gitignore updated to prevent accidental staging of binary artifacts.",
    "Use git rebase -i HEAD~N to clean up commit history before pull requests.",
    "Avoid force-pushing to shared branches; prefer rebasing private branches.",
    "Use git diff --stat to get a concise summary of modified file line counts.",
    "Run git remote -v to verify the upstream repository URL configuration.",
    "Use git fetch --prune to clean up deleted remote branch references.",
    "Configure git pull with --rebase to keep local branch history linear.",
    "Use git shortlog -sn to see commit contribution statistics by author.",
    "Create annotated git tags with git tag -a v1.0.0 for stable release points.",
    "Use git show <commit>:<file> to view a file at a specific historical commit.",
    "Run git branch -vv to check tracking relationships with remote branches.",
    "Use git branch --merged to identify local branches safe for deletion.",
    "Configure git diff.algorithm to histogram for cleaner code diffs.",
    "Use git diff -w to inspect changes while ignoring whitespace differences.",
    "FlashAgent creates milestone snapshots to ensure safe rollback capability.",
    "Verify git log before publishing releases to ensure all fixes are included.",
    "Use git archive to export a clean snapshot of code without git metadata.",
    "Use git switch -c <branch> to create and switch to a new branch quickly.",
    "Run git rev-parse --show-toplevel to get the absolute path of repo root.",
    "Use git status --ignored to see which files are currently excluded.",
    "Review merge conflicts carefully using three-way merge tools or FlashAgent.",
    "Use git rerere to automatically record and reuse conflict resolutions.",
    "Keep commit titles under 50 characters and wrap descriptions at 72 chars.",
    "Start commit titles with imperative verbs: add, fix, refactor, update.",
    "Use git log -S <symbol> to search commit history for when a symbol changed.",
    "Use git log -G <regex> to search commit diffs matching regular expressions.",
    "Tag commits associated with breaking changes clearly in commit notes.",
    "Run git fsck occasionally to verify repository database integrity.",
    "Use git count-objects -v to inspect local git object database size.",
    "Keep submodules pinned to tested commits when using git submodules.",
    "Use git submodule update --init --recursive after checking out submodules.",
    "FlashAgent avoids committing sensitive tokens or unverified configurations.",
    "Use git reflog to recover commits or branches deleted accidentally.",
    "Run git diff --cached to review what will be included in the next commit.",
    "Use git branch -m <old> <new> to rename a local git branch cleanly.",
    "Configure git commit.verbose true to review diffs directly while writing messages.",
    "Use git stash list to see all saved stashes with their creation indexes.",
    "Use git stash pop to re-apply the most recently stashed changes.",
    "Use git stash drop to discard an unneeded stash from the list.",
    "Always pull latest changes before starting a new feature branch.",
    "Use ripgrep (rg) for blazing fast code searches with automatic gitignore filtering.",
    "Use fd instead of find for intuitive syntax and default hidden-file filtering.",
    "Use jq to parse, filter, and format JSON data directly from terminal pipelines.",
    "Use xargs -P 4 to execute commands across multiple files in parallel.",
    "Use awk '{print $1, $3}' to extract specific columns from text output easily.",
    "Use sed -i 's/old/new/g' file to perform in-place text replacements in shell.",
    "Use curl -sSL to fetch web resources silently while following HTTP redirects.",
    "Use lsof -i :<port> to find which process is listening on a specific network port.",
    "Use htop or btop to monitor system CPU, memory, and process threads in real time.",
    "Use strace -e trace=file <command> to debug file access issues in Linux.",
    "Use nc -zv <host> <port> to quickly test network connectivity to remote ports.",
    "Use tar -czvf archive.tar.gz <folder> to create compressed tarball archives.",
    "Use tar -xzvf archive.tar.gz to extract compressed tarball archives.",
    "Use rsync -avz --progress to synchronize directories with resume capability.",
    "Use head -n 20 <file> to inspect the beginning of large log files quickly.",
    "Use tail -f <file> to monitor appending log files in real time.",
    "Use sort -u to sort text lines and remove duplicate entries in one command.",
    "Use uniq -c to count occurrences of duplicate lines in sorted text streams.",
    "Use cut -d',' -f1,2 to slice specific fields from CSV files on the command line.",
    "Use wc -l to count the total number of lines in a file or stream output.",
    "Use du -sh * to identify large files and folders consuming disk space.",
    "Use df -h to check available disk space across all mounted filesystems.",
    "Use which <binary> to locate the exact path of an executable in your PATH.",
    "Use type -a <command> to see if a command is a binary, alias, or shell builtin.",
    "Use tee to write terminal output to a file while still displaying it on screen.",
    "Redirect stderr to stdout using 2>&1 to capture complete command output.",
    "Discard unneeded command output by redirecting to /dev/null with > /dev/null 2>&1.",
    "Use pushd and popd to navigate between directory stacks in bash scripts.",
    "Use readlink -f <symlink> to resolve the canonical target path of symlinks.",
    "Use dirname and basename to extract folder paths and file names in shell.",
    "Use chmod +x <script> to mark shell scripts and binaries executable.",
    "Use chown user:group <file> to adjust file ownership permissions in Linux.",
    "Use env -i to run a command with an empty, isolated environment.",
    "Use watch -n 1 <command> to re-run a command every second and watch changes.",
    "Use time <command> to measure real, user, and system execution duration.",
    "Use nice and ionice to run CPU- or disk-heavy background jobs with low priority.",
    "Use pkill -f <name> to terminate all processes matching a specific name pattern.",
    "Use kill -9 <pid> as a last resort to terminate unresponsive processes.",
    "Use ps aux | grep <name> to find running process IDs matching a pattern.",
    "Use ss -tulpn to inspect active TCP and UDP listening sockets in Linux.",
    "Use uname -a to print Linux kernel version, architecture, and hostname.",
    "Use uptime to inspect system running time and 1, 5, 15 minute load averages.",
    "Use free -h to inspect total, used, and available RAM and swap space.",
    "Use lsblk to inspect block devices, partitions, and mount point hierarchies.",
    "Use file <path> to determine file format and MIME type using magic numbers.",
    "Use stat <path> to inspect detailed file inode, access, and modification timestamps.",
    "Use diff -u <file1> <file2> to generate standard unified diffs between files.",
    "Use patch -p1 < diff.patch to apply unified diff patches to codebases.",
    "Use base64 and base64 -d to encode and decode base64 strings in terminal.",
    "Use sha256sum <file> to compute cryptographic checksums for verification.",
    "Use md5sum <file> for legacy checksum generation and verification.",
    "Use fold -w 80 <file> to wrap long lines at 80 columns cleanly.",
    "Use tr '[:upper:]' '[:lower:]' to convert text streams to lowercase.",
    "Use tr -d '\\\\\\\\r' < in.txt > out.txt to strip Windows CRLF carriage returns.",
    "Use expand -t 4 <file> to convert tab characters to 4 spaces cleanly.",
    "Use unexpand -t 4 <file> to convert leading spaces back into tabs.",
    "Use column -t to format delimiter-separated text into aligned tables.",
    "Use paste -d, file1 file2 to merge corresponding lines of two files.",
    "Use split -b 10M file.bin part_ to split large files into chunks.",
    "Use shuf to randomly permute lines of text or pick random sample lines.",
    "Paste compiler error outputs verbatim in your prompt for instant fixes.",
    "Mention file paths explicitly to direct FlashAgent straight to the code.",
    "Ask FlashAgent to write unit tests first before implementing complex code.",
    "Specify verification commands in your prompt (e.g. verify with cargo test).",
    "Keep tasks focused: one coherent feature or bugfix per request is best.",
    "FlashAgent always responds in the exact same language you write to it in.",
    "Add MEMORY.md in your repository root to persist project rules permanently.",
    "Global preferences can be stored in ~/.flashagent/MEMORY.md across projects.",
    "Review diff previews carefully in Manual mode to ensure exact changes.",
    "Use /clear between unrelated tasks to keep context usage clean and fast.",
    "Provide clear acceptance criteria so the agent knows when it has succeeded.",
    "Tell FlashAgent if you prefer minimal diffs over sweeping rewrites.",
    "You can ask FlashAgent to explain unfamiliar algorithms before modifying them.",
    "Ask FlashAgent to benchmark competing implementations for performance.",
    "FlashAgent can analyze git diffs and draft structured release notes.",
    "Request step-by-step reasoning when solving subtle concurrency bugs.",
    "Mention performance budgets (e.g. < 50ms latency) in prompt requirements.",
    "Ask FlashAgent to verify edge cases: empty inputs, unicode, boundaries.",
    "If a fix fails, provide the updated error message for self-correction.",
    "You can ask FlashAgent to add descriptive doc-comments to public APIs.",
    "Ask FlashAgent to generate sample usage snippets for new libraries.",
    "Use FlashAgent to translate SQL queries into performant ORM code.",
    "FlashAgent respects repository style guides when writing new code.",
    "You can ask FlashAgent to identify dead code or unused dependencies.",
    "Ask FlashAgent to create minimal reproducible examples for stubborn bugs.",
    "Specify target compiler versions when writing code for older toolchains.",
    "Ask FlashAgent to audit code against common security vulnerabilities.",
    "You can ask FlashAgent to design architectural schemas before coding.",
    "Ask FlashAgent to suggest test cases covering happy and failure paths.",
    "FlashAgent can help you configure CI/CD workflows and automated actions.",
    "Specify memory budgets when generating data-intensive processing loops.",
    "You can ask FlashAgent to refactor monolithic functions into pure units.",
    "Ask FlashAgent to write property-based tests using proptest or quickcheck.",
    "FlashAgent can translate regex patterns into readable, documented code.",
    "Ask FlashAgent to check for memory leaks or unbounded channel buffers.",
    "You can ask FlashAgent to generate OpenAPI or JSON schema definitions.",
    "FlashAgent can help you debug HTTP API payloads and serialization bugs.",
    "Specify whether code should prioritize maximum throughput or low latency.",
    "Ask FlashAgent to verify thread safety and race-condition freedom.",
    "You can ask FlashAgent to optimize database indexes and query plans.",
    "Ask FlashAgent to review code against standard design patterns.",
    "Ask FlashAgent to write mock implementations for external web services.",
    "FlashAgent can format complex terminal outputs into readable tables.",
    "Ask FlashAgent to write integration tests verifying end-to-end flows.",
    "You can ask FlashAgent to explain complex compiler lifetime error messages.",
    "Ask FlashAgent to convert callback-based code into modern async/await.",
    "FlashAgent can help you configure cross-compilation targets for Rust.",
    "Ask FlashAgent to audit crate licenses to ensure MIT/Apache compliance.",
    "You can ask FlashAgent to generate CLI argument parsers using clap.",
    "Always test newly generated code with your project test suite.",
    "FlashAgent operates completely locally with zero cloud telemetry by default.",
    "Local inference engines like LM Studio and Ollama are supported out of the box.",
    "Context usage is monitored in real-time to avoid model context overflow.",
    "Thinking effort can be throttled to conserve local GPU inference compute.",
    "Zero emojis in source code maintains clean, professional terminal aesthetics.",
    "The input composer supports seamless multi-line editing and history navigation.",
    "Keyboard navigation supports vim-style hjkl keys across all popup menus.",
    "FlashAgent handles interrupted streams gracefully and resumes conversation.",
    "Context modal shows exact memory consumed by system prompt and tool specs.",
    "Configuration is saved in standard JSON format in ~/.flashagent/config.json.",
    "The Setup Wizard auto-discovers running local inference servers on launch.",
    "Free search mode requires zero API registration, keys, or credit cards.",
    "Terminal mouse tracking captures trackpad events for natural scrolling.",
    "Model catalog browsing uses paginated viewports for instant rendering.",
    "Live search filters model catalogs in real-time without input latency.",
    "FlashAgent respects system environment variables for proxy configurations.",
    "All file paths in error messages are clickable hyperlinks in modern terminals.",
    "Use cargo check --workspace regularly during large multi-crate refactorings.",
    "Review git diffs before pushing to avoid committing unintended scratch files.",
    "Use cargo test --doc to verify that examples in documentation compile and pass.",
    "Keep functions focused on a single responsibility for maximum testability.",
    "Write descriptive test names that clearly state the expected behavior.",
    "Use mock objects or traits to isolate unit tests from external dependencies.",
    "Run cargo build --release to produce optimized binaries for benchmarks.",
    "Use git log -p <file> to trace the complete patch history of a single file.",
    "Use git log --author=<name> to view contributions from a specific team member.",
    "Leverage cargo update to update dependencies within Cargo.lock constraints.",
    "Use git checkout -b <branch> as an alternative to git switch -c on older git.",
    "Use git commit -v to review staged changes directly in your commit message editor.",
    "Use git commit -m \\\\\\\\",
    "Use cargo metadata to inspect machine-readable project dependency graphs.",
    "Check cargo --version to ensure you are running the latest stable toolchain.",
    "Use git rev-parse --short HEAD to get the abbreviated commit hash of HEAD.",
    "Use git tag to list all existing version tags in the repository.",
    "Use cargo publish --dry-run to test crate packaging before publishing.",
    "Use git branch -d <branch> to safely delete a merged local branch.",
    "Use git branch -D <branch> to force-delete an unmerged local branch.",
    "Press Right Arrow when a ghost suggestion appears to instantly accept it into the prompt.",
    "Type /compact to summarize long conversation history and reclaim context tokens.",
    "Type /compact keep <topic> to preserve specific architectural details during compaction.",
    "FlashAgent auto-compacts context when reaching 90% capacity to prevent overflows.",
    "Press Shift+Tab to quickly switch between Accept Edits, Manual, Planning, and Auto modes.",
    "Use cargo tree -d to identify and clean up duplicate dependency versions in your crate.",
    "Use cargo watch -x test to continuously run tests as you modify Rust source files.",
    "Use cargo audit to scan your Cargo.lock for known security vulnerabilities.",
    "Use cargo expand to inspect the code generated by procedural macros and derives.",
    "Use cargo clippy -- -D warnings in CI to prevent code smells and warnings from entering main.",
    "Use git stash -u to temporarily shelve both tracked modifications and untracked files.",
    "Use git log --oneline --graph to visualize recent branch divergence and merges.",
    "Use git restore --staged <file> to unstage a file while keeping your working changes.",
    "Use git commit --amend --no-edit to update the last commit with new staged changes.",
    "Use git rebase -i HEAD~3 to squash, reorder, or edit the last three commits.",
    "Use jq -C '.' <file.json> to colorize and format JSON files in the terminal.",
    "Use ripgrep with -t rust to search only within Rust source files.",
    "Use fd -e rs to quickly find all Rust source files in a large project.",
    "Use kill -9 <pid> only as a last resort; try kill -15 (SIGTERM) first for graceful exit.",
    "Use strace or perf to profile syscall overhead and performance bottlenecks on Linux.",
    "Use valgrind or heaptrack to inspect native memory allocations and leak vectors.",
    "Write small, focused PRs to make code review faster and catch regressions early.",
    "Prefer immutable data structures and pure functions where feasible to avoid data races.",
    "Keep your main branch deployable at all times by using feature flags for work in progress.",
    "Use tracing::instrument to automatically log function arguments and execution duration.",
    "Use tokio::select! to await multiple asynchronous futures and handle cancellation cleanly.",
    "Use Arc<tokio::sync::RwLock<T>> for shared thread-safe state in asynchronous Rust.",
    "Use std::sync::atomic types for high-performance lock-free counters and flags.",
    "Avoid unwrap() in library code; bubble up errors using Result and the ? operator.",
    "Implement std::fmt::Display for domain error types to produce friendly error messages.",
    "Use anyhow for top-level application error handling and context annotation.",
    "Use thiserror to derive structured, typed error enums in Rust libraries.",
    "Use serde with #[serde(default)] to make configuration structs resilient to missing keys.",
    "Use #[derive(Default, Clone, Debug)] to provide sensible defaults and debug output.",
    "Keep unit tests fast: mock slow network and disk operations using trait objects.",
    "Use benchmark tests with criterion.rs to measure the performance impact of optimizations.",
    "Document public functions with doc-tests to keep examples working as code evolves.",
    "Structure modules hierarchically: lib.rs re-exports the clean public API.",
    "Check git status before and after running migrations to confirm expected file changes.",
    "Store persistent project guidelines in .agents/rules/ for FlashAgent to follow automatically.",
    "Global agent preferences are loaded from ~/.agents/rules/ across all workspaces.",
    "Audit records in .audit/ track every session's touched files, tests, and handoffs.",
    "FlashAgent respects your terminal color theme with TrueColor dark and warm tones.",
    "Use F5 to cycle sampling presets from Deterministic to Creative or Balanced.",
    "In Verbose mode (F2), tool inputs, outputs, and reasoning are completely transparent.",
    "Double-click or drag with mouse to select and copy text in most modern terminals.",
    "Press Esc while an autocomplete popup is open to close the popup without clearing text.",
    "Type /skills to list all active specialized skills available to FlashAgent.",
    "Press F4 at any time to cycle thinking effort between auto, low, medium, high, and off.",
    "Use Tab or Right Arrow to accept ghost completion suggestions directly into your prompt.",
    "Double-press Esc in any filter input (model picker, settings) to clear text instantly.",
    "Paste compiler error outputs or terminal stack traces directly into your prompt for instant fixes.",
    "Mention file paths explicitly in your prompt (e.g. src/main.rs) to direct FlashAgent straight to the code.",
    "Add a MEMORY.md file to your project root to permanently teach FlashAgent project rules and conventions.",
    "Put global developer preferences in ~/.flashagent/MEMORY.md to share them across all your projects.",
    "Switch to Planning mode with /mode planning to inspect code and draft plans safely without modifying files.",
    "Switch to Manual mode with /mode manual if you want to inspect and approve every file edit and command.",
    "In Accept Edits mode (default), file changes apply automatically while terminal commands require review.",
    "In /goal mode, FlashAgent runs autonomously without stopping for confirmations until finished.",
    "Press Esc at any time to interrupt an autonomous goal or long-running response safely.",
    "Type /diff to review all unstaged file modifications made during the current conversation turn.",
    "Type /undo to revert the last set of file edits made by FlashAgent using git history.",
    "Type /context to inspect token counts across system prompt, tools, and conversation history.",
    "Type /clear to reset the visible chat scrollback while keeping active project memory loaded.",
    "Ask FlashAgent to write unit tests first before implementing complex code for TDD workflows.",
    "Ask FlashAgent to explain unfamiliar algorithms or architecture before making modifications.",
    "Ask FlashAgent to benchmark competing implementations for performance in your project.",
    "Ask FlashAgent to draft structured conventional commit messages from unstaged git diffs.",
    "Ask FlashAgent to verify edge cases: empty inputs, unicode strings, and boundary values.",
    "Ask FlashAgent to optimize database indexes and query plans for slow SQL queries.",
    "Ask FlashAgent to convert callback-based code into modern async/await syntax.",
    "Ask FlashAgent to identify dead code or unused dependencies across your workspace.",
    "Use cargo check instead of cargo build during development for 3x faster compilation checks.",
    "Use cargo clippy --workspace -- -D warnings before committing to catch subtle Rust bugs.",
    "Use cargo test -- --nocapture to see printed println! outputs during unit test execution.",
    "Use cargo watch -x test to continuously re-run test suites as you modify source files.",
    "Use cargo tree -d to identify duplicate dependency versions in your Cargo.lock.",
    "Use cargo audit to scan your project dependencies for known security vulnerabilities.",
    "Use cargo expand to inspect macro-expanded Rust code during debugging.",
    "Use git diff --staged to review changes that have been added to the staging index.",
    "Use git stash -u to shelve both tracked changes and untracked files temporarily.",
    "Use git log --oneline -n 10 to view a compact summary of recent commits.",
    "Use git restore <file> to discard uncommitted working tree modifications.",
    "Use git restore --staged <file> to unstage files while preserving your edits.",
    "Use git commit --amend --no-edit to touch up the most recent commit with newly staged changes.",
    "Use git log --oneline --graph to visualize branch divergence and merges clearly.",
    "Use git rebase -i HEAD~3 to squash, reorder, or edit your last few commits.",
    "Use git bisect to locate the exact commit that introduced a bug or performance regression.",
    "Use git blame -L <start>,<end> <file> to see who modified specific lines and why.",
    "Use git worktree add ../feature-work to work on multiple branches without re-cloning.",
    "Use ripgrep (rg <pattern>) for blazing fast searches that respect .gitignore by default.",
    "Use fd instead of find for modern, intuitive file and directory searches.",
    "Use jq to parse, filter, and pretty-print JSON files directly from terminal pipelines.",
    "Use kill -15 <pid> (SIGTERM) for graceful exit; use kill -9 only as a last resort.",
    "Use curl -sSL <url> to fetch web resources silently while following HTTP redirects.",
    "Use strace -e trace=file <command> to debug file access permissions and missing paths.",
    "Use btop or htop to monitor system CPU, memory, and process threads in real time.",
    "Use tar -czvf archive.tar.gz <folder> to create compressed tar archives quickly.",
    "Use tar -xzvf archive.tar.gz to extract compressed tar archives.",
    "Use sort -u to sort text lines and eliminate duplicate entries in one command.",
    "Use wc -l <file> to count the total number of lines in a file or stream.",
    "Use which <binary> to locate the exact path of an executable in your system PATH.",
    "Use chmod +x <script> to mark shell scripts and binaries as executable.",
    "Use pkill -f <name> to terminate all background processes matching a specific name pattern.",
    "Use column -t to format delimiter-separated text into neatly aligned tables.",
    "Use xargs -P 4 to execute shell commands across multiple files in parallel.",
    "Tell the agent to handle errors with Result and ? rather than unwrap() or panics for robust code.",
    "Remind the agent to include cancellation handles for long-running async tasks.",
    "Ask the agent to verify that all file handles and database connections are properly closed.",
    "Remind the agent to check that numeric calculations cannot overflow.",
    "Ask the agent to generate markdown documentation from code comments and public symbols.",
    "Tell the agent to preserve existing code formatting and comment styles when editing.",
    "Remind the agent to avoid introducing unnecessary third-party dependencies.",
    "Check the .audit/ directory to inspect past decisions, tests, and file modifications across sessions.",
    "Use Ctrl+Shift+C and Ctrl+Shift+V to copy and paste text in modern Linux terminals.",
    "Press Ctrl+R to regenerate the last model response from scratch.",
    "Press Tab on an empty input prompt to open the Settings menu.",
    "Press F1 to open the Context Inspector and inspect token usage by section.",
    "Press F2 to cycle Verbose mode between off, last turn, and all reasoning cards.",
    "Press F3 to open the real-time searchable Model Selection menu.",
    "Press F4 to cycle Thinking Effort between off, low, medium, and high.",
    "Press F5 to open the Sampling Presets menu and switch LLM sampling profiles.",
    "Press Shift+Tab to quickly cycle through permission modes: Bypass, Auto, Ask-First, Plan-First.",
    "Press Ctrl+O to expand or collapse the most recent reasoning card in the chat.",
    "Press Alt+O to expand or collapse all reasoning cards across the session.",
    "Press Esc while the model is responding to interrupt streaming immediately.",
    "Press PageUp and PageDown to scroll through chat history smoothly.",
    "Hold Shift and scroll with your trackpad or mouse wheel for accelerated scrolling.",
    "Press Ctrl+C to cancel current text input without exiting FlashAgent.",
    "Press Ctrl+V or Ctrl+Shift+V to paste multi-line snippets into the input field.",
    "Type / to open the slash-command autocomplete menu with instant fuzzy search.",
    "Type /goal <task> to start an autonomous execution loop that solves tasks without stopping.",
    "Type /compact to summarize conversation history and free up context space.",
    "Type /compact keep <topic> to preserve specific architectural details during context compaction.",
    "Type /diff to inspect git modifications made by FlashAgent before committing.",
    "Type /undo to revert the last batch of file modifications using git history.",
    "Type /skills to list all currently loaded skills and their triggering rules.",
    "Type /audit to review the history of session decisions and verification records.",
    "Type /settings to view or configure backend URLs, models, and temperature presets.",
    "Type /model to search and activate available local models dynamically.",
    "Type /verbose all to keep all reasoning blocks and tool cards expanded across turns.",
    "Type /verbose last to expand only the most recent reasoning and tool cards.",
    "Type /verbose off to keep reasoning cards collapsed for a cleaner chat view.",
    "Type /clear to reset current conversation scrollback and start a fresh session.",
    "Type /help to display all keyboard shortcuts, slash commands, and active configuration.",
    "Type /exit or /quit to terminate the FlashAgent session immediately.",
    "In Ask-First permission mode, FlashAgent requests approval before executing bash commands.",
    "In Plan-First permission mode, FlashAgent outlines steps before performing any changes.",
    "In Bypass permission mode, FlashAgent executes workspace tools with maximum speed.",
    "In Auto permission mode, safe read-only operations run automatically while mutations ask.",
    "The status bar turns amber at 75% context capacity and red at 90% capacity.",
    "When context capacity reaches 90%, FlashAgent triggers automatic context compaction.",
    "The status bar displays real-time token generation speed in tokens per second (tg).",
    "The status bar shows prefix KV cache hit rate to verify efficient prompt cache reuse.",
    "The status bar displays MTP draft token acceptance percentage for speculative decoding.",
    "FlashAgent uses bracketed paste mode to handle large pasted snippets without mangling.",
    "FlashAgent automatically reflows text and adjusts card borders on terminal window resize.",
    "Code blocks in assistant responses are syntax-highlighted with language-specific palettes.",
    "Tool calls in flight show an animated braille spinner and elapsed execution time.",
    "Diff previews display added lines in green and removed lines in red with line numbers.",
    "Pass --yes on startup to skip the directory trust confirmation prompt automatically.",
    "Pass --model <name> on startup to launch FlashAgent with a specific model directly.",
    "Pass --url <endpoint> on startup to connect FlashAgent to a custom OpenAI-compatible server.",
    "Pass --setup on startup to relaunch the interactive terminal onboarding wizard.",
    "Pass --headless on startup to run FlashAgent in non-interactive pipeline mode.",
    "Pass --verbose on startup to start FlashAgent with expanded reasoning cards by default.",
    "Pass --permission <mode> on startup to pre-select bypass, auto, ask-first, or plan-first.",
    "Run flashagent --version from your shell to inspect the current build version and target.",
    "Skill definitions live in .agents/skills/<name>.md with Trigger, Steps, and Verify fields.",
    "Audit entries live in .audit/YYYY-MM-DD-<slug>.md as an append-only verifiable record.",
    "Audit records include Status (PASS/FAIL), Decision, Files touched, and exact Verification.",
    "Under gate rule B0, ROADMAP.md takes absolute precedence over audit logs.",
    "FlashAgent automatically recovers from server 400 errors by learning supported thinking presets.",
    "The Gemma sampling preset uses temperature 1.0, top_p 0.95, and min_p 0.03 for Gemma 4.",
    "The Qwen sampling preset uses temperature 0.7, top_p 0.80, and presence_penalty 0.05.",
    "The Coding sampling preset uses temperature 0.2 and top_p 0.10 for deterministic code output.",
    "The Deterministic sampling preset uses temperature 0.0 for perfectly reproducible outputs.",
    "The Creative sampling preset uses temperature 1.1 and top_p 0.98 for brainstorming.",
    "The Low-Latency sampling preset disables thinking and maximizes KV cache reusability.",
    "Use Option::as_deref() to convert an Option<String> into an Option<&str> without allocating.",
    "Use Option::as_deref_mut() to obtain a mutable slice reference from an Option<Vec<T>>.",
    "Use Option::take() to take ownership of a value out of an Option, leaving None in its place.",
    "Use Option::replace() to put a new value into an Option and return the old one.",
    "Use Option::transpose() to turn an Option<Result<T, E>> into a Result<Option<T>, E> cleanly.",
    "Use Option::flatten() to collapse nested Option<Option<T>> into a single Option<T>.",
    "Use Option::zip(other) to combine two Options into an Option<(T, U)> when both are Some.",
    "Use Option::filter(predicate) to retain Some only if the predicate returns true.",
    "Use Option::unwrap_or_else(fn) to compute default values lazily only when None.",
    "Use Option::ok_or(err) to convert an Option<T> into a Result<T, E> when missing.",
    "Use Option::ok_or_else(fn) to produce an error lazily when converting Option to Result.",
    "Use Option::is_some_and(predicate) to test a predicate on Some without unwrap.",
    "Use Result::transpose() to convert a Result<Option<T>, E> into an Option<Result<T, E>>.",
    "Use Result::map_err() to transform the error type before propagating with the ? operator.",
    "Use Result::and_then() to chain fallible operations without nested match blocks.",
    "Use Result::or_else() to handle an error by attempting an alternative fallible operation.",
    "Use Result::inspect() to view a successful value without consuming or altering the Result.",
    "Use Result::inspect_err() to log or debug an error without consuming the Result.",
    "Use Result::is_ok_and(predicate) to test a condition on Ok without branching.",
    "Use std::mem::take(&mut val) to replace a value with its Default while taking ownership.",
    "Use std::mem::replace(&mut dest, src) to swap in a new value and return the original.",
    "Use std::mem::swap(&mut a, &mut b) to swap two mutable references without heap copies.",
    "Use std::mem::forget(val) when intentionally skipping the Drop implementation for a value.",
    "Use std::mem::size_of::<T>() to check the compile-time byte size of any type in Rust.",
    "Use std::mem::align_of::<T>() to inspect the byte alignment requirement of a type.",
    "Use std::mem::discriminant(&val) to compare enum variants without checking payloads.",
    "Use Cow::Borrowed to avoid heap allocation when borrowing read-only string slices.",
    "Use Cow::into_owned() to convert copy-on-write data into owned heap memory only if needed.",
    "Preallocate vectors with Vec::with_capacity(n) when the item count is known in advance.",
    "Use Vec::retain(|item| ...) to filter elements in-place without allocating a new vector.",
    "Use Vec::retain_mut(|item| ...) to modify elements in-place while deciding retention.",
    "Use Vec::dedup() on a sorted vector to remove consecutive duplicate elements in-place.",
    "Use Vec::dedup_by() to remove consecutive duplicate items based on a custom comparison.",
    "Use Vec::drain(..) to remove a range of elements as an iterator while retaining capacity.",
    "Use Vec::split_off(at) to divide a vector into two separate vectors at a given index.",
    "Use Vec::swap_remove(idx) for O(1) removal when element order does not need to be kept.",
    "Use Vec::shrink_to_fit() to release unused buffer memory after large vector truncations.",
    "Use Vec::spare_capacity_mut() to safely initialize unwritten buffer memory in a Vec.",
    "Use Vec::extend_from_slice(&slice) for fast, bulk memcpy of contiguous slice data.",
    "Use Box::leak(b) to promote dynamically allocated heap data to a 'static reference.",
    "Use Box::pin(val) to pin a future or self-referential struct to the heap.",
    "Use Box::into_raw(b) when transferring ownership of heap memory across FFI boundaries.",
    "Use Rc::clone(&rc) rather than rc.clone() to clearly indicate reference counter increments.",
    "Use Arc::clone(&arc) to make atomic reference count increments obvious during code review.",
    "Use Arc::strong_count(&arc) to inspect the current number of active owners of a value.",
    "Use Arc::weak_count(&arc) to inspect the count of non-owning weak references.",
    "Use Arc::downgrade(&arc) to create a Weak pointer that prevents cyclic reference leaks.",
    "Use Weak::upgrade() to safely acquire a temporary Arc only if the value still exists.",
    "Use Arc::make_mut(&mut arc) to clone the inner value only if shared (copy-on-write).",
    "Use Arc::get_mut(&mut arc) to obtain mutable access if there are no other active owners.",
    "Use RefCell::borrow() for runtime-checked immutable borrowing in single-threaded code.",
    "Use RefCell::borrow_mut() for runtime-checked mutable borrowing in single-threaded code.",
    "Use Cell<T> for interior mutability of Copy types without borrow-checking runtime overhead.",
    "Use Cell::set(&val) to replace the stored value without needing a mutable reference.",
    "Use std::sync::OnceLock::get_or_init() for thread-safe lazy global initialization.",
    "Use std::sync::OnceLock::set() to initialize a global variable exactly once safely.",
    "Use std::sync::LazyLock::new(init_fn) in Rust 1.80+ for declarative lazy static values.",
    "Use the HashMap::entry() API (or_insert / and_modify) to look up and mutate in one pass.",
    "Use HashMap::with_capacity(n) to prevent costly hash table rehashing on insertion.",
    "Use HashMap::retain() to filter entries in-place without reallocating map buckets.",
    "Use BTreeMap::range(start..end) to iterate efficiently over sorted key subsets.",
    "Use HashSet::is_subset(&other) to verify if all items belong to another set.",
    "Use HashSet::intersection(&other) to compute shared elements between two collections.",
    "Use HashSet::difference(&other) to find items present in the first set but not second.",
    "Use std::collections::BinaryHeap::peek() to inspect the maximum priority item in O(1).",
    "Use std::collections::VecDeque for efficient FIFO queues and ring buffers.",
    "Use String::with_capacity(n) to avoid reallocations when building large text strings.",
    "Use String::push_str(&slice) rather than format! or + for efficient string concatenation.",
    "Use String::retain(|c| ...) to filter characters in-place inside an existing String.",
    "Use str::rsplit_once(sep) to split a filename or path from its last separator cleanly.",
    "Use str::split_once(sep) to parse key-value pairs without allocating vector slices.",
    "Use str::strip_prefix(p) to safely test and discard expected prefix substrings.",
    "Use str::strip_suffix(s) to strip file extensions or suffixes without slicing panics.",
    "Use str::trim_matches(|c| ...) to strip custom delimiters from both ends of a slice.",
    "Use str::lines() to iterate over text lines handling both Unix \\n and Windows \\r\\n.",
    "Use Iterator::enumerate() to get 0-indexed counters alongside iterated items.",
    "Use Iterator::filter_map() to combine filtering and mapping in a single iterator pass.",
    "Use Iterator::flat_map() to map elements to iterators and flatten the results.",
    "Use Iterator::peekable() to inspect the next element without advancing the iterator.",
    "Use Iterator::fold(init, fn) to accumulate elements into a single aggregate result.",
    "Use Iterator::scan(state, fn) for stateful streaming transformations over an iterator.",
    "Use Iterator::chain(other) to concatenate two iterators of the same item type sequentially.",
    "Use Iterator::rposition(pred) on double-ended iterators to find the last matching index.",
    "Use Iterator::fuse() to ensure that an iterator continues returning None once exhausted.",
    "Use Iterator::by_ref() to borrow an iterator mutably so it can be resumed afterwards.",
    "Use Iterator::collect() with type annotations to collect into Vec, HashSet, or Result.",
    "Use Iterator::step_by(step) to skip items at regular intervals during iteration.",
    "Use Iterator::zip(other) to pair corresponding elements from two iterators.",
    "Use Iterator::take_while(pred) to yield elements until the predicate returns false.",
    "Use Iterator::skip_while(pred) to drop prefix items until the condition is met.",
    "Use Iterator::all(pred) to check if every element satisfies a predicate with early exit.",
    "Use Iterator::any(pred) to test if at least one item satisfies a condition with early exit.",
    "Use std::path::Path::join() to construct cross-platform filesystem paths safely.",
    "Use std::path::Path::parent() to access directory containers without manual string parsing.",
    "Use std::path::Path::extension() to inspect file extensions safely as an OsStr.",
    "Use std::path::Path::file_name() to extract the final component of a path safely.",
    "Use std::path::Path::is_absolute() to distinguish relative paths from absolute paths.",
    "Use PathBuf::pop() to navigate up one directory level without allocating a new buffer.",
    "Use std::fs::read_to_string() for reading entire text files into memory in one call.",
    "Use std::fs::write() for writing complete byte slices or strings to disk atomically.",
    "Wrap std::fs::File in std::io::BufReader to avoid making thousands of small syscalls.",
    "Wrap file streams in std::io::BufWriter to batch write operations for peak I/O throughput.",
    "Prefer passing borrowed slices (&str, &[T]) rather than owned types (&String, &Vec<T>) to functions.",
    "Implement the AsRef<Path> trait on function arguments to accept Path, PathBuf, &str, or String seamlessly.",
    "Implement the AsRef<str> trait on API parameters to accept both &str and String without extra clones.",
    "Implement From<T> rather than Into<T>; implementing From automatically provides the Into implementation.",
    "Implement TryFrom<T> for fallible type conversions to return explicit, descriptive error types.",
    "Derive Default on configuration structs so callers can instantiate with Struct { field, ..Default::default() }.",
    "Use #[derive(Debug, Clone, PartialEq, Eq)] on domain structs and enums to ease testing and debugging.",
    "Add #[must_use] to functions returning critical results, tokens, or locks that should not be ignored.",
    "Mark pure functions or small accessors with #[inline] to encourage cross-crate compiler inlining.",
    "Use #[cold] on error handling branches to help the CPU branch predictor optimize the happy path.",
    "Apply #[repr(transparent)] on newtype wrappers that wrap a single inner field for zero FFI overhead.",
    "Use #[repr(C)] on structs shared across C FFI boundaries to guarantee standard C memory layout.",
    "Use #[repr(align(64))] on multithreaded atomic structs to prevent cache-line false sharing.",
    "Mark public library enums with #[non_exhaustive] so adding variants in future minor releases is non-breaking.",
    "Use the newtype pattern (e.g. struct UserId(u64)) to prevent accidentally mixing up identifier types.",
    "Use PhantomData<T> to indicate logical ownership or lifetime relationships without storing values.",
    "Use compile-time type-state patterns to guarantee state machine transitions cannot occur out of order.",
    "Prefer pattern matching with matches!(expr, Pattern) instead of verbose match blocks for boolean checks.",
    "Use dbg!(expression) during development to print file, line, and value while returning the value unchanged.",
    "Use eprintln! for diagnostic logs and warnings so standard output (stdout) remains clean for piping.",
    "Use format_args! when delegating to logging or formatting functions to avoid allocating intermediate Strings.",
    "Use unreachable!(\"message\") to document and guard code branches that should be mathematically impossible.",
    "Use todo!(\"reason\") as a temporary placeholder during incremental development and stub design.",
    "Avoid using panic! or unwrap() in production libraries; propagate informative errors with Result instead.",
    "Use the thiserror crate for defining clean, strongly-typed library error enums with derive macros.",
    "Use the anyhow crate in application code or binaries for ergonomic error handling with context chaining.",
    "Use anyhow::Context (with_context) to attach high-level task descriptions to low-level I/O errors.",
    "Use std::sync::atomic::Ordering::Relaxed for counters and stats where synchronization ordering is unneeded.",
    "Use std::sync::atomic::Ordering::Acquire and Release for lock-free message passing and publishing pointers.",
    "Use std::sync::atomic::Ordering::SeqCst only when a globally consistent sequential order is strictly required.",
    "Avoid holding a MutexGuard or RwLockReadGuard across await points in async functions to prevent deadlocks.",
    "Use parking_lot::Mutex instead of std::sync::Mutex for smaller memory footprint and faster uncontended locks.",
    "Use parking_lot::RwLock for read-heavy shared data structures to allow concurrent reader threads.",
    "Always acquire multiple locks in a consistent, predefined global order across threads to prevent deadlocks.",
    "Use std::sync::Condvar with a Mutex to put worker threads to sleep until a specific condition is signaled.",
    "Implement the Drop trait to release external resources, temporary directories, or FFI handles automatically.",
    "Use RAII (Resource Acquisition Is Initialization) to ensure cleanup logic runs reliably even during panics.",
    "Use #[serde(default)] on struct fields so missing fields in JSON payloads automatically fallback to Default.",
    "Use #[serde(rename = \"field_name\")] to map between snake_case Rust identifiers and camelCase JSON keys.",
    "Use #[serde(skip_serializing_if = \"Option::is_none\")] to omit None fields from serialized JSON payloads.",
    "Use #[serde(borrow)] on Cow<'a, str> or &'a str fields for zero-copy deserialization of JSON text.",
    "Use serde_json::from_reader on buffered file streams to parse JSON without loading the whole file into RAM.",
    "Use serde_json::to_writer on buffered writers for streaming serialization of large data structures.",
    "Run cargo check --workspace during development for 3x faster compilation feedback than full builds.",
    "Run cargo clippy --workspace -- -D warnings to enforce zero lints across all workspace crates.",
    "Run cargo clippy --fix to automatically apply machine-applicable compiler and clippy suggestions.",
    "Run cargo test --workspace to run every unit, integration, and doc test across all crates.",
    "Run cargo test -- --nocapture to print stdout and println! output while tests are executing.",
    "Run cargo test <filter> to run only tests whose function names match the given substring.",
    "Run cargo test --test <filename> to execute a specific integration test file in the tests/ directory.",
    "Run cargo test --doc to execute only documentation code examples found in docstrings.",
    "Run cargo test --benches to compile benchmarks in test mode to verify they do not crash.",
    "Run cargo bench to execute performance benchmarks and track statistical timing differences.",
    "Run cargo update -p <crate_name> to update a single dependency to its latest semver-compatible version.",
    "Run cargo tree -d to identify duplicate dependency versions across your Cargo.lock file.",
    "Run cargo tree -i <crate_name> to find out which dependencies pull in a specific transitive crate.",
    "Run cargo tree --depth 1 to inspect only your direct, first-level workspace dependencies.",
    "Run cargo audit to scan your Cargo.lock dependencies against the RustSec Advisory Database.",
    "Run cargo outdated to check for newer semver-incompatible major releases of your dependencies.",
    "Run cargo expand to inspect macro-expanded Rust code during complex macro debugging.",
    "Run cargo llvm-cov --html to generate an interactive HTML report showing code test coverage.",
    "Run cargo flamegraph --bin <name> to profile your Rust binary and generate an interactive SVG flamegraph.",
    "Run cargo bloat --release -n 20 to view the top 20 functions consuming binary executable space.",
    "Run cargo machete to detect unused dependencies declared in your Cargo.toml files.",
    "Run cargo deny check to verify license compliance, banned crates, and security advisories in CI.",
    "Run cargo nextest run to execute tests in parallel with an isolated process model and clean output.",
    "Run cargo watch -x check -x test to automatically re-check and re-test code on every file save.",
    "Run cargo clean -p <crate_name> to purge build artifacts for a single crate without rebuilding all.",
    "Run cargo doc --open --no-deps to build and open HTML documentation for your workspace crates only.",
    "Run cargo doc --document-private-items to inspect documentation for private modules and functions.",
    "Run cargo metadata --format-version 1 | jq . to query workspace structure and crate dependency graphs.",
    "Run cargo vendor to download all project dependencies into a local vendor directory for offline builds.",
    "Run cargo publish --dry-run to verify package packaging and metadata before publishing to crates.io.",
    "Add lto = \"fat\" to [profile.release] in Cargo.toml to enable cross-crate Link Time Optimization.",
    "Set codegen-units = 1 in [profile.release] to maximize compiler optimizations at the cost of compile time.",
    "Set panic = \"abort\" in [profile.release] to reduce binary size and eliminate unwinding tables.",
    "Set strip = true in [profile.release] to remove debug symbols and symbol tables from release binaries.",
    "Set opt-level = 3 in [profile.release] to enable maximum performance optimizations in release builds.",
    "Set opt-level = \"s\" or \"z\" in [profile.release] to optimize binaries for small executable file size.",
    "Set debug = 1 in release profiles to include line-number tables for profiling without huge file bloat.",
    "Use workspace inheritance ([workspace.dependencies]) to share identical dependency versions across crates.",
    "Set default-features = false when depending on crates to avoid compiling unneeded transitive dependencies.",
    "Export RUST_BACKTRACE=1 in your shell to display full stack traces whenever a Rust panic occurs.",
    "Export RUST_LOG=debug to view detailed logging when using env_logger or tracing-subscriber.",
    "Pass RUSTFLAGS=\"-C target-cpu=native\" when building locally to compile with all host CPU vector extensions.",
    "Run rustup default stable to ensure your environment is running the official stable Rust compiler.",
    "Run rustup component add clippy rust-analyzer rust-src to install essential development components.",
    "Run rustc --explain E0382 to view the official documentation and code examples for any compiler error code.",
    "Use tokio::spawn to launch concurrent background async tasks on Tokio's multi-threaded runtime.",
    "Use tokio::select! to await multiple concurrent futures and handle whichever branch completes first.",
    "Use tokio::join! to await multiple futures concurrently and receive all their results together.",
    "Use tokio::try_join! to run fallible futures concurrently and abort early if any returns an Err.",
    "Use tokio::task::spawn_blocking for CPU-heavy tasks or blocking I/O to avoid stalling async worker threads.",
    "Never call std::thread::sleep in an async function; use tokio::time::sleep instead to avoid blocking threads.",
    "Use tokio::time::timeout(duration, future) to enforce maximum latency boundaries on async requests.",
    "Use tokio::time::interval(duration) for periodic background heartbeat ticks and recurring timers.",
    "Use tokio::task::yield_now().await in tight computation loops to allow other cooperative tasks to run.",
    "Use bounded channels (tokio::sync::mpsc::channel(N)) to apply backpressure and prevent unbounded RAM growth.",
    "Use tokio::sync::broadcast for fan-out messaging where multiple subscribers receive every published event.",
    "Use tokio::sync::watch for sharing frequently updated state where receivers only care about the latest value.",
    "Use tokio::sync::oneshot for transferring a single result between an async producer and a consumer.",
    "Use tokio::sync::Semaphore to limit concurrent access to expensive resources like connections or GPUs.",
    "Use tokio::sync::Notify to signal one or more waiting async tasks without transferring payload data.",
    "Use tokio_util::sync::CancellationToken for structured, hierarchical cancellation across async tasks.",
    "Be aware of cancellation safety: futures dropped inside tokio::select! discard their in-progress state.",
    "Use futures::stream::StreamExt::next(&mut stream).await to consume elements from asynchronous streams.",
    "Use futures::stream::StreamExt::buffer_unordered(N) to run up to N stream futures concurrently.",
    "Use futures::stream::StreamExt::chunks(size) to batch incoming stream items into fixed-size chunks.",
    "Wrap dynamic async streams in BoxStream<'static, T> to abstract concrete stream types across crate boundaries.",
    "Use tokio::signal::ctrl_c() to listen for SIGINT and trigger graceful application shutdown.",
    "Rust does not support async Drop; provide explicit async fn close(self) methods for fallible resource cleanup.",
    "Use tracing::instrument on async functions to attach structured span attributes to logs automatically.",
    "Use tokio-console during profiling to diagnose async task blocking, starvation, and queue lag in real time.",
    "Modern x86_64 and ARM64 CPU cache lines are 64 bytes; align data structures to prevent false sharing.",
    "Use SIMD vectorization (AVX2, NEON) to process multiple numeric values in a single CPU clock cycle.",
    "Use copy-on-write (COW) data structures to share read-only data across threads without cloning on read.",
    "Use memory mapping (memmap2 crate) for reading large multi-gigabyte files without copying into heap RAM.",
    "Design network protocols with explicit message framing (e.g. 4-byte length prefix) to avoid stream splitting bugs.",
    "Use exponential backoff with full jitter when retrying failed network requests to prevent thundering herds.",
    "Apply the circuit breaker pattern to prevent repeated cascading calls to unresponsive backend endpoints.",
    "Attach unique request IDs or correlation IDs to every incoming query for end-to-end distributed tracing.",
    "In microservices, distinguish between liveness probes (process alive) and readiness probes (ready for traffic).",
    "Design distributed mutations with idempotency keys so re-executing failed requests does not duplicate effects.",
    "Use git commit --fixup <commit_hash> to create a fixup commit intended for autosquash rebasing.",
    "Use git rebase -i --autosquash <base> to automatically squash fixup commits into their target commits.",
    "Use git commit --amend --no-edit to quickly incorporate newly staged changes into the previous commit.",
    "Use git commit -v to review the unified diff of staged changes directly inside your commit message editor.",
    "Use git branch -vv to inspect all local branches with their upstream tracking status and commit counts.",
    "Use git branch -m <old_name> <new_name> to rename a local git branch cleanly.",
    "Use git branch --merged to identify feature branches that have already been integrated into main.",
    "Use git branch --no-merged to view feature branches that still contain unmerged commits.",
    "Use git branch -d <branch> to delete a merged branch safely without risking data loss.",
    "Use git branch -D <branch> to force-delete an unmerged branch when intentionally discarding work.",
    "Use git stash push -m \"description\" to label temporary stashes with clear contextual names.",
    "Use git stash list to inspect all saved stashes along with their index numbers and descriptions.",
    "Use git stash show -p stash@{0} to view the full unified patch contained within a specific stash.",
    "Use git stash pop to apply the most recent stash onto your working directory and remove it from the list.",
    "Use git stash apply to apply a stash onto your working tree while keeping it saved in the stash list.",
    "Use git stash drop stash@{0} to delete a specific stash entry that is no longer needed.",
    "Use git stash branch <new_branch> to create and checkout a new branch populated with the stashed changes.",
    "Use git stash -u (or --include-untracked) to stash both modified tracked files and untracked files.",
    "Use git stash -a (or --all) to stash all tracked files, untracked files, and ignored files.",
    "Use git rebase -i HEAD~N to interactively squash, edit, reorder, or drop your last N commits.",
    "Use git rebase --onto <newbase> <oldbase> <branch> to transplant a branch onto a different base commit.",
    "Use git rebase --abort to immediately cancel a conflicted rebase and restore your original branch state.",
    "Use git rebase --continue to proceed with a rebase after resolving conflict markers in affected files.",
    "Use git rebase --skip to bypass a problematic commit during a rebase when its changes are already present.",
    "Use git cherry-pick -n <commit> to apply a commit's changes to your working tree without committing.",
    "Use git cherry-pick --abort to cancel a conflicted cherry-pick and restore your working tree cleanly.",
    "Use git bisect start followed by git bisect bad and good to find the commit that introduced a regression.",
    "Use git bisect run <test_script> to automate binary search across commits to find regressions hands-free.",
    "Use git bisect reset to exit bisect mode and return your repository to the branch you started on.",
    "Use git worktree add ../feature-branch <branch> to work on multiple branches simultaneously in parallel dirs.",
    "Use git worktree list to inspect all active git worktrees and their checked-out branches.",
    "Use git worktree remove ../feature-branch to delete a worktree directory once feature work is complete.",
    "Use git worktree prune to clean up internal administrative references to deleted worktree directories.",
    "Use git diff --staged (or --cached) to review only changes that have been added to the staging index.",
    "Use git diff --word-diff to highlight inline word modifications rather than entire changed lines.",
    "Use git diff --stat to view a high-level summary of changed files and lines added/deleted.",
    "Use git add -p to interactively review and stage individual hunks of code rather than entire files.",
    "Use git restore -p to interactively review and discard specific changed hunks in your working tree.",
    "Use git restore --staged <file> to unstage changes from the index while keeping working tree edits intact.",
    "Use git restore <file> to discard unstaged working tree changes and reset the file to HEAD.",
    "Use git clean -nd to perform a dry run showing which untracked directories and files would be deleted.",
    "Use git clean -fd to forcefully remove untracked directories and files from your working tree.",
    "Use git log --oneline --graph --decorate to visualize branch topology, merges, and tags in your terminal.",
    "Use git log -S <string> (pickaxe search) to find commits that added or removed a specific text string.",
    "Use git log -G <regex> to search commit diffs for changes matching a regular expression.",
    "Use git log -L <start>,<end>:<file> to view the complete evolution and commit history of specific line ranges.",
    "Use git log --author=\"<name>\" to filter project commit history to contributions by a specific author.",
    "Use git log --since=\"2 weeks ago\" to review commits made within a specific recent time window.",
    "Use git shortlog -sn to generate a ranked contributor list ordered by total number of commits.",
    "Use git reflog to locate and recover commits or branches that were accidentally lost after rebase or reset.",
    "Use git blame -w <file> to see line-by-line commit authorship while ignoring whitespace-only formatting changes.",
    "Use git blame -C <file> to detect code that was copied or moved from other files in the repository.",
    "Use git show <commit>:<file> to inspect the contents of a file at a specific commit without checking it out.",
    "Use git tag -a v1.0.0 -m \"Release v1.0.0\" to create an annotated, signed release tag with metadata.",
    "Use git push origin --tags to publish all locally created annotated tags to the remote repository.",
    "Use git fetch -p (or --prune) to delete local tracking branches that no longer exist on the remote server.",
    "Use git remote prune origin to remove stale remote-tracking branch references from your local repo.",
    "Use git remote update to refresh all remote-tracking branches across all configured remotes.",
    "Use git submodule update --init --recursive to fetch and initialize all nested repository submodules.",
    "Run git config --global rerere.enabled true to automatically remember and reapply merge conflict resolutions.",
    "Run git config --global pull.rebase true to rebase local commits on git pull instead of creating merge commits.",
    "Run git config --global init.defaultBranch main to set main as the default branch name for new repositories.",
    "Run git maintenance start to configure automatic background optimization and prefetching for your repo.",
    "Run git fsck to verify the structural integrity of the local git object database and find dangling blobs.",
    "Run git gc --prune=now to aggressively clean up unreachable objects and pack loose files to save disk space.",
    "Use ripgrep (rg <pattern>) for blazing fast code searches that respect .gitignore rules by default.",
    "Use rg -i <pattern> to perform case-insensitive searches across your entire workspace.",
    "Use rg -w <word> to match only complete, isolated words rather than substrings.",
    "Use rg -F <literal> to search for literal strings containing special regex characters without escaping.",
    "Use rg -C 3 <pattern> to print 3 lines of leading and trailing context around each search match.",
    "Use rg -A 2 -B 2 <pattern> to specify different amounts of context after and before matches.",
    "Use rg -g \"*.rs\" <pattern> to restrict your ripgrep search to files matching a specific glob pattern.",
    "Use rg -t rust <pattern> to quickly filter searches by programming language file type.",
    "Use rg --type-list to inspect all supported built-in file types recognized by ripgrep.",
    "Use rg -l <pattern> (or --files-with-matches) to print only the filenames containing matches.",
    "Use rg --files-without-match <pattern> to list files that do not contain the specified search term.",
    "Use rg --hidden to search files and directories normally hidden by leading dots.",
    "Use rg --no-ignore to search through files and directories normally excluded by .gitignore.",
    "Use rg -U --multiline to search across multiple lines using multi-line regular expression patterns.",
    "Use rg --replace \"replacement\" to preview text replacements across matching files in your terminal.",
    "Use fd instead of find for modern, intuitive file searches with colored output and gitignore respect.",
    "Use fd -e rs to quickly locate all Rust source files across your project directory tree.",
    "Use fd -t d <name> to restrict your search results exclusively to directories.",
    "Use fd -t f <name> to restrict your search results exclusively to regular files.",
    "Use fd -H to include hidden files and dotfiles in your fd search results.",
    "Use fd -E target to exclude build artifact directories from your search scope.",
    "Use fd -x <command> to execute a command on each discovered file path in parallel.",
    "Use fd -X <command> to pass all discovered file paths to a single invocation of a command.",
    "Use jq . <file.json> to format, colorize, and pretty-print JSON documents in your terminal.",
    "Use jq -r \".field\" to extract string fields without enclosing JSON quotation marks.",
    "Use jq -c . to output JSON data in a compact, single-line format ideal for log processing.",
    "Use jq \".items[] | .name\" to iterate over an array of objects and print the name property of each.",
    "Use jq \"map(.id)\" to transform an array of objects into an array containing only their id values.",
    "Use jq \"select(.status == \\\"error\\\")\" to filter JSON streams down to entries matching a criteria.",
    "Use jq \"keys\" to inspect all top-level keys available in a JSON object.",
    "Use jq \"has(\\\"key\\\")\" to check for the existence of a specific property in a JSON object.",
    "Use jq -s . (slurp) to read multiple separate JSON objects into a single combined JSON array.",
    "Use jq --arg key val '.[$key]' to pass shell variables safely into jq filter expressions.",
    "Use fzf to interactively fuzzy-search through file paths, commands, and git commits.",
    "Use git checkout $(git branch | fzf) to select and checkout local git branches interactively.",
    "Use kill -9 $(ps -ef | fzf | awk '{print $2}') to interactively select and terminate rogue processes.",
    "Use fzf --preview 'cat {}' to preview file contents in a side panel while fuzzy-searching.",
    "Use awk '{print $1, $NF}' to extract the first and last columns of whitespace-separated text output.",
    "Use awk -F',' '{print $2}' to parse comma-separated text and extract specific CSV fields.",
    "Use awk '{sum += $1} END {print sum}' to compute the arithmetic sum of numbers in a stream.",
    "Use sed 's/pattern/replacement/g' <file> to replace all occurrences of a pattern in a text stream.",
    "Use sed -i 's/old/new/g' <file> to perform in-place text replacements on files in Linux.",
    "Use xargs -P $(nproc) to execute shell commands across multiple inputs in parallel using all CPU cores.",
    "Use xargs -I {} <cmd> {} to substitute input lines into specific placeholder positions in commands.",
    "Use sort -u to sort lines of text and eliminate duplicate entries in a single step.",
    "Use sort -h (human numeric sort) to sort file sizes formatted with K, M, and G suffixes correctly.",
    "Use sort -k2,2nr to sort tabular data numerically in reverse order by the second column.",
    "Use uniq -c to count consecutive occurrences of identical lines in a sorted text stream.",
    "Use comm -12 <(sort file1) <(sort file2) to find lines that appear in both files efficiently.",
    "Use cut -d':' -f1 /etc/passwd to extract usernames from colon-separated system files.",
    "Use paste -d',' file1 file2 to merge corresponding lines of two files separated by commas.",
    "Use tr -d '\\r' <file> > clean_file to strip Windows carriage-return characters from text files.",
    "Use fold -w 80 <file> to wrap long lines of text to a maximum width of 80 characters.",
    "Use column -t -s',' to format delimiter-separated text into neatly aligned terminal tables.",
    "Use tee -a <file> to write command output to a file while simultaneously printing it to the terminal.",
    "Use ss -tulpn to list all active listening TCP and UDP sockets with their associated process PIDs.",
    "Use ps aux --sort=-%cpu | head -n 10 to display the top 10 processes consuming CPU resources.",
    "Use ps aux --sort=-%mem | head -n 10 to display the top 10 processes consuming system RAM.",
    "Use pgrep -l <name> to find process IDs matching a program name along with process titles.",
    "Use pkill -f <pattern> to terminate background processes matching a specific command line pattern.",
    "Use kill -15 <pid> (SIGTERM) for graceful process exit; reserve kill -9 (SIGKILL) as a last resort.",
    "Use kill -HUP <pid> to instruct daemons to reload their configuration files without restarting.",
    "Use free -h to check available, used, and cached system RAM and swap space in human-readable units.",
    "Use df -h to check disk space usage and free space across all mounted filesystems.",
    "Use du -sh * | sort -h to identify which files and directories consume the most disk space.",
    "Use nc -zv <host> <port> to quickly test if a remote TCP port is open and accepting connections.",
    "Use curl -sSL -w \"\\nHTTP %{http_code} in %{time_total}s\\n\" to benchmark HTTP endpoint response times.",
    "Use strace -e trace=openat,read,write -p <pid> to inspect live file and network syscalls of a process.",
    "Use dmesg -T | tail -n 20 to view the most recent Linux kernel ring buffer messages with timestamps.",
    "Use vmstat 1 5 to monitor system memory, swap, I/O, and CPU activity in 1-second intervals.",
    "Use iostat -xz 1 to monitor disk I/O utilization, throughput, and await latency in real time.",
    "Use tmux new -s <name> to create named terminal multiplexer sessions that survive SSH disconnects.",
    "Use tmux attach -t <name> to reconnect to an existing running tmux session.",
    "Press Ctrl+b % in tmux to split the current window vertically into two panes.",
    "Press Ctrl+b \" in tmux to split the current window horizontally into two panes.",
    "Press Ctrl+b z in tmux to toggle zooming the active pane to fullscreen and back.",
    "Press Ctrl+b c in tmux to create a new window within your active session.",
    "Press Ctrl+b , in tmux to rename the active window for clear workspace organization.",
    "Use perf record -g -- <command> followed by perf report to profile CPU hotspots and call graphs.",
    "Use perf stat -- <command> to measure instructions per cycle (IPC), branch misses, and cache misses.",
    "Use heaptrack to profile memory allocations, detect memory leaks, and locate heap allocation hotspots.",
    "Use valgrind --leak-check=full to track down memory leaks and invalid memory accesses in C/FFI code.",
    "Use gdb -p <pid> to attach the GNU debugger to a running process to inspect backtraces and memory.",
    "In gdb, use watch <variable> to halt program execution whenever the value of a variable changes.",
    "In lldb, use thread backtrace all to inspect call stacks across all active threads in a crashed process.",
    "Compile with -Zsanitizer=address to catch out-of-bounds memory accesses and use-after-free bugs.",
    "Compile with -Zsanitizer=thread to catch data races across concurrent threads during test execution.",
    "Compile with -Zsanitizer=undefined to detect undefined behavior like integer overflows and misaligned pointers.",
    "Write deterministic property-based tests using the proptest crate to discover edge-case input crashes.",
    "Use the criterion crate for statistically rigorous micro-benchmarks that account for CPU clock jitter.",
    "Use cargo-fuzz to run coverage-guided fuzz testing against untrusted parser inputs.",
    "Follow defensive programming: validate data at untrusted system boundaries, then trust internal invariants.",
    "Follow fail-fast principles: assert invariants early rather than letting corrupted state propagate.",
    "Avoid premature optimization: profile first with realistic workloads to find actual performance bottlenecks.",
    "Keep functions focused on a single responsibility: functions that do one thing are easier to test and verify.",
    "Favor flat control flow with early returns over deeply nested if-else ladders for readability.",
    "Use semantic versioning (SemVer): increment MAJOR for breaking changes, MINOR for features, PATCH for fixes.",
    "Use char::is_ascii_digit() for fast, branchless checking of ASCII numeric digits.",
    "Use char::is_alphanumeric() to check if a Unicode character is a letter or number.",
    "Use char::to_lowercase() to obtain an iterator of lowercase characters for Unicode.",
    "Use saturating arithmetic (e.g. u32::saturating_add) to prevent integer overflows without panicking.",
    "Use u32::saturating_sub to safely subtract values without underflowing below zero.",
    "Use u32::checked_div() to guard against division by zero errors by returning an Option.",
    "Use wrapping arithmetic (e.g. u32::wrapping_add) when intentional modular arithmetic is desired.",
    "Use u32::count_ones() (popcount) to count the number of set bits in a single CPU instruction.",
    "Use u32::leading_zeros() to count leading zero bits using dedicated hardware instructions.",
    "Use u32::trailing_zeros() to count trailing zero bits for fast bit manipulation.",
    "Use u32::to_be_bytes() to convert integers into big-endian byte arrays for network wire formats.",
    "Use u32::to_le_bytes() to serialize integers into little-endian byte arrays for disk storage.",
    "Use u32::from_be_bytes(bytes) to parse integers from big-endian network byte buffers safely.",
    "Use u32::from_le_bytes(bytes) to deserialize integers from little-endian disk buffers.",
    "Use std::num::NonZeroUsize to represent non-zero values and enable null-pointer optimizations.",
    "Use std::num::NonZeroU32 so Option<NonZeroU32> occupies the exact same memory size as u32.",
    "Use std::time::Duration::from_millis(ms) to represent millisecond timeouts with strong types.",
    "Use std::time::Duration::from_secs_f64(secs) to construct durations from fractional seconds.",
    "Use std::time::Instant::now() to measure elapsed wall-clock execution time accurately.",
    "Use Instant::elapsed() to query how much time has passed since a timer was started.",
    "Use std::process::Command::new(\"binary\") to spawn child processes with configured arguments.",
    "Pass individual arguments with Command::arg() rather than shell strings to prevent injection bugs.",
    "Use Command::env(\"KEY\", \"VAL\") to pass custom environment variables to spawned child processes.",
    "Use Command::output() to run a child process to completion and collect stdout and stderr.",
    "Use Command::status() when you only need to check the exit status code of a child command.",
    "Use std::process::ExitStatus::success() to check if a process terminated with exit code 0.",
    "Use std::env::var(\"NAME\") to read environment variables as Result<String, VarError> safely.",
    "Use std::env::var_os(\"NAME\") to read environment variables that may contain non-UTF-8 bytes.",
    "Use std::env::current_dir() to obtain the active working directory of the running process.",
    "Use std::env::temp_dir() to find the designated system temporary directory cross-platform.",
    "Use std::env::args() to iterate over command-line arguments passed to the running program.",
    "Use std::thread::sleep(duration) in synchronous worker threads to yield execution temporarily.",
    "Use std::thread::spawn(closure) to create native OS threads for long-running synchronous work.",
    "Use std::thread::available_parallelism() to detect the number of logical CPU cores.",
    "Use std::sync::mpsc::channel() for multi-producer single-consumer thread communication.",
    "Use std::sync::mpsc::sync_channel(bound) to create bounded synchronous thread channels.",
    "Use serde for high-performance, format-agnostic serialization and deserialization in Rust.",
    "Use serde_json::Value for parsing arbitrarily structured or schema-less JSON payloads.",
    "Use serde_yaml or serde_yaml_ng to parse and validate YAML configuration files in Rust.",
    "Use toml::from_str to deserialize Cargo.toml or custom TOML application configuration files.",
    "Use tracing instead of println! or log for structured, span-aware diagnostic instrumentation.",
    "Use tracing_subscriber::fmt::init() to set up a clean, colored terminal log subscriber.",
    "Use clap::Parser with derive macros for declarative, strongly-typed command-line argument parsing.",
    "Use clap::Subcommand enums to define clean CLI tool subcommands with custom arguments.",
    "Use reqwest for ergonomic HTTP client requests with async streaming and connection pooling.",
    "Use reqwest::Client::builder().pool_idle_timeout() to tune connection reuse for HTTP calls.",
    "Use hyper for low-level, high-throughput HTTP/1 and HTTP/2 protocol implementations in Rust.",
    "Use axum or actix-web to build ergonomic, modular, and blazing fast async web services in Rust.",
    "Use tower::Service to build composable middleware layers for rate limiting, timeouts, and logging.",
    "Use sqlx for compile-time verified asynchronous SQL queries with zero runtime ORM overhead.",
    "Use diesel for type-safe, compile-time checked SQL query building and database migrations.",
    "Use redis crate with deadpool-redis for asynchronous, connection-pooled Redis caching.",
    "Use crossbeam-channel for high-throughput, multi-producer multi-consumer synchronous channels.",
    "Use crossbeam::epoch for lock-free memory reclamation in high-performance concurrent data structures.",
    "Use rayon::prelude::* to parallelize CPU-bound iterator computations across all available CPU cores.",
    "Use par_iter() from rayon to process large vectors in parallel with minimal code changes.",
    "Use dashmap::DashMap for a high-concurrency concurrent hash map with fine-grained bucket locking.",
    "Use regex::Regex::new() once and cache it in a LazyLock to avoid expensive regex recompilation.",
    "Use regex-automata when you need low-level control over DFA construction or streaming regex matches.",
    "Use tempfile::NamedTempFile to create secure temporary files that are deleted automatically on Drop.",
    "Use tempfile::tempdir() to create isolated temporary scratch directories for automated unit tests.",
    "Use walkdir::WalkDir for recursive filesystem directory traversal with symlink handling.",
    "Use glob::glob() to search for files matching wildcard patterns like src/**/*.rs.",
    "Use notify crate to listen for live filesystem modification events and trigger hot-reloads.",
    "Use bitflags::bitflags! macro to define type-safe C-style bitwise flag combinations.",
    "Use semver::Version to parse and compare Semantic Versioning strings according to SemVer 2.0.",
    "Use url::Url::parse() to validate, normalize, and manipulate HTTP and custom URI schemes safely.",
    "Use uuid::Uuid::new_v4() to generate cryptographically secure random version 4 UUIDs.",
    "Use chrono or time crate for timezone-aware date and time manipulation and ISO-8601 formatting.",
    "Use ring or rustls for modern, high-performance cryptography and TLS without OpenSSL dependencies.",
    "Use rand::Rng::gen_range() to generate random numbers within a specified boundary range.",
    "Use rand::seq::SliceRandom::choose() to pick random elements from slices safely.",
    "Use zeroize crate to securely wipe sensitive cryptographic keys and passwords from RAM upon drop.",
    "Use pin-project or pin-project-lite to project pinned struct fields safely without unsafe code.",
    "Use async-trait crate when defining traits with async fn methods that need dynamic dispatch.",
    "Use bytes::Bytes for reference-counted, zero-copy byte slice buffers in networking code.",
    "Use bytes::BytesMut for efficient in-place buffer accumulation and split operations.",
    "Use flate2 or zstd crate for fast streaming compression and decompression of binary payloads.",
    "Use bincode for compact, ultra-fast binary serialization between Rust processes.",
    "Use postcard for zero-allocation, extremely compact binary serialization on embedded systems.",
    "Use memchr crate for SIMD-accelerated byte searching in string and binary buffers.",
    "Use ahash or fxhash for non-cryptographic, ultra-fast hash table lookups in memory-bound apps.",
    "Use parking_lot::RwLockUpgradableReadGuard to atomically upgrade a read lock to a write lock.",
    "Use smol or async-std as lightweight alternatives to Tokio when minimal runtime footprint is desired.",
    "Use pretty_assertions::assert_eq! in tests to display clean colored diffs on assertion failures.",
    "Use mockall crate to generate mock implementations of traits for isolated unit testing.",
    "Use insta crate for snapshot testing of complex serialized structures and formatted text.",
    "Use proptest or quickcheck to automatically generate hundreds of random test cases for property testing.",
    "Linux signals: SIGTERM (15) requests clean shutdown, allowing processes to run cleanup handlers.",
    "Linux signals: SIGKILL (9) terminates a process immediately without running exit handlers or flushing I/O.",
    "Linux signals: SIGHUP (1) is conventionally used to tell daemon processes to reload configuration files.",
    "Linux signals: SIGINT (2) is generated by pressing Ctrl+C in the controlling terminal.",
    "Linux signals: SIGQUIT (3) is generated by pressing Ctrl+\\ and requests process exit with core dump.",
    "Linux signals: SIGUSR1 (10) and SIGUSR2 (12) are user-defined signals for custom process messaging.",
    "Linux signals: SIGPIPE (13) is sent when writing to a pipe or socket whose reading end has closed.",
    "Linux signals: SIGCHLD (17) is sent to a parent process when any of its child processes terminates.",
    "Linux signals: SIGSTOP (19) pauses a process immediately and cannot be caught or ignored.",
    "Linux signals: SIGCONT (18) resumes execution of a previously paused process.",
    "Inspect /proc/<pid>/status to check memory usage, thread count, and signal masks of a process.",
    "Inspect /proc/<pid>/cmdline to see the exact command line arguments used to launch a process.",
    "Inspect /proc/<pid>/environ to view the environment variables of any running process.",
    "Inspect /proc/<pid>/fd/ to view all file descriptors currently opened by a specific process.",
    "Inspect /proc/<pid>/limits to check resource limits like max open files (nofile) for a process.",
    "Inspect /proc/cpuinfo to check CPU model name, core count, cache sizes, and hardware flags.",
    "Inspect /proc/meminfo to see detailed system-wide RAM metrics like MemAvailable and Buffers.",
    "Inspect /proc/loadavg to view system load averages over 1, 5, and 15-minute time windows.",
    "Use ulimit -n to check the maximum number of open file descriptors allowed per process.",
    "Use ulimit -c unlimited to enable core dumps when debugging unexpected process segmentation faults.",
    "Use sysctl -w to tune Linux kernel parameters at runtime without rebooting the system.",
    "Use journalctl -u <service_name> -f to tail systemd service logs in real time.",
    "Use journalctl -p err -b to inspect error messages logged by systemd during the current boot.",
    "Use systemctl status <service_name> to check if a systemd background daemon is active and running.",
    "Use systemctl restart <service_name> to restart a system service cleanly.",
    "Use systemctl reload <service_name> to reload service configuration without dropping connections.",
    "Use ip addr show (or ip a) to inspect active network interfaces, IP addresses, and MAC addresses.",
    "Use ip route show to view the current Linux network routing table and default gateway.",
    "Use ping -c 4 <host> to test basic network connectivity and packet round-trip time.",
    "Use traceroute or tracepath <host> to inspect the intermediate network hops to a destination.",
    "Use dig <domain> or nslookup <domain> to query DNS records and diagnose domain resolution.",
    "Use mtr <host> for an interactive, continuous traceroute that tracks packet loss per hop.",
    "Use ethtool <interface> to check physical network link speed, duplex mode, and auto-negotiation.",
    "Use iptables -L -n -v or nft list ruleset to view active firewall filtering rules and packet counters.",
    "Use tcpdump -i any -n 'port 80 or port 443' to capture live HTTP and HTTPS network packets.",
    "Use tshark (terminal Wireshark) for advanced command-line network packet analysis and filtering.",
    "Use iperf3 -c <server_ip> to benchmark maximum network bandwidth between two machines.",
    "Use openssl s_client -connect <host>:443 to inspect TLS certificates and handshake details.",
    "Use openssl x509 -in cert.crt -text -noout to inspect the validity dates and SANs of a certificate.",
    "Use chown -R user:group <directory> to change file ownership recursively across a tree.",
    "Use chmod 600 <file> to restrict sensitive file access (like SSH keys) exclusively to the owner.",
    "Use chmod 755 <dir> to allow the owner to write while allowing others to read and traverse directories.",
    "Use umask 022 to ensure new files are created with standard read permissions for group and others.",
    "Use ls -lh to view file sizes in human-readable megabytes and gigabytes in directory listings.",
    "Use ls -lt to list directory contents sorted by modification time with the newest files first.",
    "Use ls -la to view all files in a directory including hidden dotfiles and permissions.",
    "Use tree -L 2 to view a clean hierarchical tree diagram of subdirectories up to 2 levels deep.",
    "Use readlink -f <file> to resolve symbolic links and print the canonical absolute path.",
    "Use ln -s <target> <link_name> to create a symbolic link pointing to a file or directory.",
    "Use stat <file> to inspect exact inode numbers, access timestamps, and filesystem block allocations.",
    "Use file <filename> to identify file types and encoding formats using magic number analysis.",
    "Use md5sum or sha256sum <file> to calculate cryptographic checksums for verifying file integrity.",
    "Use Ctrl+A in bash/zsh to jump the cursor immediately to the beginning of the command line.",
    "Use Ctrl+E in bash/zsh to jump the cursor immediately to the end of the command line.",
    "Use Ctrl+U in bash to delete all text from the current cursor position to the beginning of the line.",
    "Use Ctrl+K in bash to delete all text from the current cursor position to the end of the line.",
    "Use Ctrl+W in bash to delete the word immediately before the cursor position.",
    "Use Alt+B in bash to move the cursor backward one word at a time.",
    "Use Alt+F in bash to move the cursor forward one word at a time.",
    "Use Ctrl+L in terminal to clear the screen while preserving your current command line buffer.",
    "Use Ctrl+R in terminal to reverse-search through your shell command execution history.",
    "Use !! in bash to execute the previous command again (e.g. sudo !! to rerun with root privileges).",
    "Use !$ in bash to reference the last argument of the previous command on the new line.",
    "Use !:1 in bash to reference the first argument of the preceding command.",
    "Use ^old^new in bash to rerun the previous command replacing the first occurrence of 'old' with 'new'.",
    "Use history | grep <keyword> to locate previously executed commands by keyword.",
    "Add a leading space before a shell command to prevent it from being saved to bash history.",
    "Use set -e (or set -o errexit) in shell scripts to immediately exit if any command returns an error.",
    "Use set -u (or set -o nounset) in shell scripts to treat unset environment variables as errors.",
    "Use set -x (or set -o xtrace) in shell scripts to print commands and arguments as they are executed.",
    "Use set -o pipefail in shell scripts so pipelines fail if any command in the pipeline fails.",
    "Combine set -euo pipefail at the top of bash scripts for robust, defensive script execution.",
    "Use shasum -a 256 <file> to compute SHA-256 hashes on macOS and BSD systems.",
    "Use base64 -d to decode base64-encoded strings directly from terminal pipelines.",
    "Use echo -n 'text' | base64 to encode text strings to base64 without trailing newlines.",
    "Use diff -u file1 file2 to generate unified diff patches between two text files.",
    "Use patch -p1 < patchfile.diff to apply unified diff patches to source repositories.",
    "Use watch -n 1 '<command>' to execute a command repeatedly every second and watch output changes.",
    "Use time <command> to measure real (wall clock), user (CPU), and sys (kernel) time taken by a program.",
    "Use timeout 10s <command> to forcefully kill a command if it takes longer than 10 seconds to finish.",
    "Use nohup <command> & to run background processes that survive terminal closure and logout.",
    "Use disown -h %1 to detach a running job from the terminal so closing the shell does not terminate it.",
    "Use jobs -l to view active background jobs managed by the current interactive shell session.",
    "Use fg %1 to bring background job number 1 into the foreground of the current terminal.",
    "Use bg %1 to resume execution of a suspended job in the background.",
    "Press Ctrl+Z in terminal to suspend the currently running foreground process with SIGTSTP.",
    "Use nice -n 10 <command> to launch CPU-intensive background tasks with reduced scheduling priority.",
    "Use ionice -c 3 <command> to run batch disk maintenance tasks using idle I/O priority.",
    "Use rename 's/old/new/' *.txt to batch-rename files using Perl regular expressions.",
    "Use gzip -d <file.gz> (or gunzip) to decompress gzip-compressed files.",
    "Use bzip2 -d <file.bz2> (or bunzip2) to decompress bzip2-compressed files.",
    "Use xz -d <file.xz> (or unxz) to decompress xz-compressed files with high compression ratios.",
    "Use zip -r archive.zip <folder> to create cross-platform zip archives from directories.",
    "Use unzip -l archive.zip to inspect the file listing of a zip archive without extracting it.",
    "Use unzip -q archive.zip to extract zip archives silently without flooding terminal scrollback.",
    "Write unit tests that test behavior and public APIs rather than internal implementation details.",
    "Structure tests using the Arrange-Act-Assert (AAA) pattern for clear readability and maintenance.",
    "Keep unit tests independent and hermetic: no test should rely on the state or execution order of another.",
    "Mock external network I/O, email services, and payment gateways in test environments to ensure speed.",
    "Use test fixtures and factories to generate consistent, valid test data across test suites.",
    "Treat compiler warnings as errors in CI pipelines to prevent code rot and subtle bugs from accumulating.",
    "Automate code formatting with rustfmt, prettier, or black in pre-commit hooks to eliminate style debates.",
    "Enforce lint rules automatically in CI to catch antipatterns and deprecated syntax early.",
    "Keep pull requests small and focused (under 400 lines of diff) for thorough, high-quality code reviews.",
    "Write meaningful commit messages that explain WHY a change was made rather than just WHAT was touched.",
    "Use conventional commit prefixes (feat, fix, refactor, test, chore) for automated changelog generation.",
    "Apply the single responsibility principle: a module should have one, and only one, reason to change.",
    "Apply the open-closed principle: software entities should be open for extension, but closed for modification.",
    "Apply the dependency inversion principle: depend upon abstractions rather than concrete implementations.",
    "Prefer composition over inheritance to create flexible, decoupled object models.",
    "Favor pure functions without side effects for business calculations to ensure testability.",
    "Isolate stateful side effects (I/O, database, network) to the outer edges of your system architecture.",
    "Design systems with graceful degradation: when non-essential services fail, core features should remain usable.",
    "Implement health check endpoints (/healthz) that report both service liveness and database readiness.",
    "Use structured JSON logging in production environments for easy ingestion by log aggregators.",
    "Log at appropriate levels: ERROR for actionable failures, WARN for anomalies, INFO for business milestones.",
    "Never log sensitive user data, passwords, API tokens, or personally identifiable information (PII).",
    "Use correlation IDs across distributed services to trace individual user requests across system boundaries.",
    "Apply rate limiting at API gateways to protect downstream backend databases from denial-of-service spikes.",
    "Design API schemas with backwards compatibility in mind to avoid breaking older deployed clients.",
    "Deprecate API fields with clear timelines and documentation before removing them in major releases.",
    "Use feature flags to decouple code deployment from feature release and enable progressive rollouts.",
    "Implement canary deployments to verify new software versions on a small percentage of production traffic.",
    "Implement database migrations that are backwards-compatible so old and new application code can coexist.",
    "Always test database rollbacks and recovery procedures before performing major schema changes in production.",
    "Use EXPLAIN ANALYZE in PostgreSQL or MySQL to inspect the actual query execution plan and cost.",
    "Add database indexes to columns frequently used in WHERE clauses, JOIN conditions, and ORDER BY fields.",
    "Avoid SELECT * in production queries; request only the specific columns your application needs.",
    "Use database transactions (BEGIN...COMMIT) to ensure groups of related mutations execute atomically.",
    "Set appropriate transaction isolation levels (Read Committed, Serializable) based on consistency needs.",
    "Use connection pooling (e.g. pgBouncer, deadpool) to avoid the high overhead of establishing new DB sockets.",
    "Use batch INSERT statements or COPY commands when importing thousands of rows into databases.",
    "Use database pagination with cursor-based pagination (WHERE id > last_id) rather than large OFFSET values.",
    "Avoid N+1 query patterns by eagerly loading related associations using JOINs or batch lookups.",
    "Use Redis caching with explicit TTL expiration for read-heavy, infrequently changing database queries.",
    "Apply the cache-aside pattern: check cache first, fetch from DB on miss, and populate cache with TTL.",
    "Use Redis hashes (HSET/HGET) to store structured objects efficiently with lower memory overhead.",
    "Use Redis sorted sets (ZADD/ZRANGE) for implementing real-time leaderboards and sliding-window rate limiters.",
    "Use Redis pub/sub or Redis streams for lightweight asynchronous event broadcasting between services.",
    "Set memory eviction policies (e.g. allkeys-lru) in Redis so caches automatically evict when RAM is full.",
    "Use VACUUM ANALYZE in PostgreSQL to reclaim storage and update query planner table statistics.",
    "Monitor PostgreSQL pg_stat_activity to detect long-running or stalled queries holding table locks.",
    "Set query timeouts (statement_timeout in Postgres) to kill rogue runaway queries automatically.",
    "Use foreign keys with ON DELETE CASCADE or ON DELETE RESTRICT to enforce relational referential integrity.",
    "Normalize database schemas to third normal form (3NF) to eliminate data redundancy and update anomalies.",
    "Selectively denormalize read-heavy analytics tables to avoid expensive multi-table joins at runtime.",
    "Use i8 for signed 8-bit integers when storing compact byte-level delta values.",
    "Use u8 for raw byte values, binary protocol headers, and ASCII character codes.",
    "Use i16 for signed 16-bit audio samples or compact coordinate offsets.",
    "Use u16 for network port numbers (0..65535) to model domain invariants safely.",
    "Use i32 as the default signed integer type for general-purpose mathematical arithmetic.",
    "Use u32 for counting items where values are guaranteed non-negative and fit in 4GB.",
    "Use i64 for Unix epoch timestamps in milliseconds to avoid the year 2038 overflow.",
    "Use u64 for file sizes in bytes, hash digest values, and high-precision nanoseconds.",
    "Use i128 for high-precision financial arithmetic or cryptographic large-number math.",
    "Use u128 for storing 128-bit UUIDs or IPv6 addresses compactly as native integers.",
    "Use isize for pointer-offset calculations and memory buffer indexing differences.",
    "Use usize for indexing slices and representing sizes of objects in memory.",
    "Use f32 for 32-bit single-precision floating point numbers in graphics and games.",
    "Use f64 for double-precision scientific simulations and high-accuracy calculations.",
    "Implement Default for structs so callers can instantiate with sensible starting defaults.",
    "Derive Debug on all domain types to enable formatting with the {:?} format specifier.",
    "Implement Display for types that have a natural, user-facing string representation.",
    "Derive Clone to provide explicit value duplication using the .clone() method.",
    "Implement Copy for small, bitwise-duplicable types that live entirely on the stack.",
    "Derive PartialEq to enable equality comparisons using the == and != operators.",
    "Implement Eq for types where equality is reflexive, symmetric, and transitive.",
    "Derive PartialOrd to enable ordering comparisons (<, <=, >, >=) between instances.",
    "Implement Ord for types with a total, unambiguous ordering across all values.",
    "Derive Hash on types to allow using them as keys in HashMaps and HashSets.",
    "Types implementing Send can be safely transferred across thread boundaries in Rust.",
    "Types implementing Sync can be safely referenced from multiple concurrent threads.",
    "Types implementing Unpin can be safely moved even after being pinned in memory.",
    "Implement Deref to allow smart pointer types to transparently expose methods of inner types.",
    "Implement Drop to release external resources or file handles when an object goes out of scope.",
    "Use git cat-file -p <hash> to pretty-print the contents of any raw git object.",
    "Use git cat-file -t <hash> to check the object type (blob, tree, commit, tag).",
    "Use git hash-object -w <file> to compute SHA-1 and write raw objects to the git database.",
    "Use git ls-files to list all files currently tracked in the git staging index.",
    "Use git ls-files -m to list tracked files that have local working tree modifications.",
    "Use git ls-files -o --exclude-standard to list untracked files respecting .gitignore.",
    "Use git rev-parse --show-toplevel to find the root directory of the current repository.",
    "Use git rev-parse --abbrev-ref HEAD to get the name of the active branch in scripts.",
    "Use git rev-parse HEAD to get the full 40-character SHA-1 commit hash of HEAD.",
    "Use git diff-tree --no-commit-id --name-only -r <commit> to list files modified in a commit.",
    "Use git write-tree to record the current staging index as a tree object in plumbing scripts.",
    "Use git update-index --assume-unchanged <file> to ignore local changes to tracked files.",
    "Use git update-index --no-assume-unchanged <file> to resume tracking file changes.",
    "Use git symbolic-ref HEAD to check which branch ref HEAD currently points to.",
    "Use git check-ref-format <branch_name> to validate branch names before creating them.",
    "Use curl -I <url> to fetch and inspect only the HTTP response headers of a web page.",
    "Use curl -v <url> to view the full request headers, TLS handshake, and response headers.",
    "Use curl -s <url> for silent execution without showing progress meters or error meters.",
    "Use curl -o <filename> <url> to download web content directly to a specified destination file.",
    "Use curl -O <url> to download files using the remote filename automatically.",
    "Use curl -H 'Authorization: Bearer <token>' to pass authentication headers to REST APIs.",
    "Use curl -d '{\"key\":\"value\"}' -H 'Content-Type: application/json' for POST requests.",
    "Use curl -L <url> to instruct curl to automatically follow 301 and 302 HTTP redirects.",
    "Use curl -X DELETE <url> to send explicit HTTP DELETE verbs to REST endpoints.",
    "Use curl -u 'user:pass' <url> to perform HTTP Basic Authentication.",
    "Use curl --compressed to request gzip/deflate compression from web servers.",
    "Use curl -k (or --insecure) to test endpoints with self-signed SSL certificates locally.",
    "Use wget -q <url> to download files silently in the background.",
    "Use wget -c <url> to resume an interrupted file download where it left off.",
    "Use wget -r -np <url> to recursively download directory contents without ascending.",
    "Use head -n 20 <file> to view only the first 20 lines of a large file.",
    "Use tail -n 20 <file> to inspect the last 20 lines of a log or text file.",
    "Use tail -f <file> to continuously follow new lines appended to a log file in real time.",
    "Use wc -w <file> to count the total number of words in a document.",
    "Use wc -c <file> to count the total number of bytes in a file.",
    "Use grep -v <pattern> to invert match results and display lines that do not match.",
    "Use grep -E (or egrep) to use extended regular expression syntax for pattern matching.",
    "Use grep -o <pattern> to print only the matching substrings rather than entire lines.",
    "Use sort -r to reverse the sorting order of text lines from descending to ascending.",
    "Use sort -V to perform natural version number sorting (e.g. v1.2 before v1.10).",
    "Use rev <file> to reverse the character order of each line in a text stream.",
    "Use nl -ba <file> to number every line of a text file including empty lines.",
    "Use expand -t 4 <file> to convert tab characters into 4 spaces in text files.",
    "Use unexpand -t 4 <file> to convert leading spaces into tab characters.",
    "In Rust, the borrow checker guarantees memory safety without garbage collection overhead.",
    "In Rust, lifetime tracking guarantees memory safety without garbage collection overhead.",
    "In Rust, affine type system guarantees memory safety without garbage collection overhead.",
    "In Rust, RAII resource management guarantees memory safety without garbage collection overhead.",
    "In Rust, exclusive mutable references (&mut T) guarantees memory safety without garbage collection overhead.",
    "In Rust, shared immutable references (&T) guarantees memory safety without garbage collection overhead.",
    "In Rust, zero-cost abstractions guarantees memory safety without garbage collection overhead.",
    "In Rust, strict ownership semantics guarantees memory safety without garbage collection overhead.",
    "In Rust, bounds checking on slice indexing guarantees memory safety without garbage collection overhead.",
    "In Rust, the lack of null pointers guarantees memory safety without garbage collection overhead.",
    "In Rust, safe interior mutability via RefCell guarantees memory safety without garbage collection overhead.",
    "In Rust, automatic struct drop ordering guarantees memory safety without garbage collection overhead.",
    "In Rust, const fn execution is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, generic monomorphization is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, macro expansion is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, trait resolution is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, associated type projection is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, array size verification is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, static assertion checking is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, derive macro code generation is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, cfg conditional compilation is evaluated entirely at compile time with zero runtime cost.",
    "In Rust, type inference is evaluated entirely at compile time with zero runtime cost.",
    "Use Option::unwrap_or_default() when you need to return the type's Default if an Option is None in Rust.",
    "Use Result::unwrap_or_default() when you need to fallback to Default on any Err in a Result in Rust.",
    "Use Iterator::fold_while when you need to accumulate items with an early stopping condition in Rust.",
    "Use std::hint::black_box() when you need to prevent compiler optimizations in micro-benchmarks in Rust.",
    "Use std::hint::spin_loop() when you need to yield CPU resources inside low-latency busy-wait loops in Rust.",
    "Use std::hint::unreachable_unchecked() when you need to inform the optimizer a branch is impossible in unsafe code in Rust.",
    "Use std::alloc::alloc() when you need to perform low-level manual heap allocation with custom layouts in Rust.",
    "Use std::alloc::dealloc() when you need to manually deallocate raw heap memory pointers in Rust.",
    "Use std::ptr::copy() when you need to perform memmove on potentially overlapping raw pointer buffers in Rust.",
    "Use std::ptr::copy_nonoverlapping() when you need to perform memcpy on guaranteed disjoint raw pointers in Rust.",
    "Use std::ptr::read() when you need to read a value out of a raw pointer without running Drop in Rust.",
    "Use std::ptr::write() when you need to write a value to an uninitialized raw memory location in Rust.",
    "Use std::ptr::null() when you need to represent a null raw pointer for C FFI compatibility in Rust.",
    "Use std::ptr::null_mut() when you need to represent a mutable null raw pointer for C FFI bindings in Rust.",
    "Use std::ffi::CStr when you need to borrow null-terminated C strings without allocating heap memory in Rust.",
    "Use std::ffi::CString when you need to create owned, null-terminated C strings to pass to C libraries in Rust.",
    "Use std::ffi::OsStr when you need to handle OS-native filesystem strings across Windows and Unix safely in Rust.",
    "Use std::ffi::OsString when you need to store owned OS-native strings without forcing UTF-8 conversion in Rust.",
    "Use std::panic::catch_unwind() when you need to catch panics across FFI boundaries or thread pools in Rust.",
    "Use std::panic::set_hook() when you need to register custom panic reporting hooks to log backtraces in Rust.",
    "Use std::sync::Barrier when you need to synchronize multiple threads at a common rendezvous point in Rust.",
    "Use std::thread::current().id() when you need to inspect the unique ThreadId of the running OS thread in Rust.",
    "Use std::thread::park() when you need to put the current thread to sleep until explicitly unparked in Rust.",
    "Use std::thread::Thread::unpark() when you need to wake up a specifically parked OS worker thread in Rust.",
    "Use std::io::copy() when you need to stream bytes directly from a Read stream to a Write stream in Rust.",
    "Use std::io::sink() when you need to discard all written bytes with zero overhead in test mocks in Rust.",
    "Use std::io::empty() when you need to provide an immediately exhausted empty Read stream in Rust.",
    "Use std::io::repeat() when you need to generate an infinite stream repeating a single byte value in Rust.",
    "Use std::io::Cursor when you need to wrap in-memory byte buffers (&[u8]) to implement Read and Write in Rust.",
    "Use std::io::Lines when you need to iterate lazily over buffered text lines from any Read source in Rust.",
    "Use std::io::Split when you need to iterate over chunks of bytes split by a specific delimiter in Rust.",
    "Use std::io::Take when you need to limit the maximum number of bytes read from an underlying stream in Rust.",
    "Use std::io::Chain when you need to concatenate two Read streams into a single seamless reader in Rust.",
    "Use git rev-list --count HEAD to count the total number of commits in the current branch history.",
    "Use git rev-list --max-parents=0 HEAD to find the root (initial) commit of a git repository.",
    "Use git merge-base <branch1> <branch2> to find the common ancestor commit of two diverging branches.",
    "Use git merge-base --is-ancestor <commit1> <commit2> to test if commit1 is an ancestor of commit2 in scripts.",
    "Use git commit --dry-run to preview which changes would be committed without creating a commit.",
    "Use git commit -S -m 'message' to create cryptographically GPG-signed commits for verification.",
    "Use git tag -v <tag_name> to verify the GPG cryptographic signature of an annotated git tag.",
    "Use git tag -d <tag_name> to delete a local git tag that was created by mistake.",
    "Use git push origin --delete <tag_name> to delete a remote tag from the upstream git server.",
    "Use git push origin --delete <branch> to delete a remote branch from the upstream repository.",
    "Use git push origin HEAD to push the active branch to its remote tracking branch conveniently.",
    "Use git push origin HEAD:refs/for/main to push commits to code review systems like Gerrit.",
    "Use git fetch origin <branch> to fetch updates for a single branch without downloading all branches.",
    "Use git pull --ff-only to ensure git pull only completes if the branch can be fast-forwarded.",
    "Use git clone --depth 1 <url> to perform a shallow clone downloading only the latest commit to save time.",
    "Use git clone --branch <name> --single-branch <url> to clone exclusively a single branch from a repository.",
    "Use git clone --filter=blob:none <url> to perform a blobless clone to download file content on-demand.",
    "Use git clone --filter=tree:0 <url> to perform a treeless clone for ultra-fast CI build checkouts.",
    "Use git remote -v to inspect all configured remote repository URLs for fetch and push.",
    "Use git remote set-url origin <new_url> to update the remote URL for the origin repository.",
    "Use git remote add upstream <url> to add an upstream remote repository when working on forks.",
    "Use git remote rename <old> <new> to rename a configured git remote repository reference.",
    "Use git status -s (or --short) to display working tree status in a compact two-column format.",
    "Use git status --ignored to display files currently ignored by .gitignore rules.",
    "Use git diff HEAD~1 HEAD to inspect the exact changes introduced by the most recent commit.",
    "Use git diff <commit1>..<commit2> to view the complete diff between two arbitrary git commits.",
    "Use git diff <branch1>...<branch2> to view changes on branch2 since it diverged from branch1.",
    "Use git log --grep='<pattern>' to search commit messages for specific keywords or issue numbers.",
    "Use git log --all --graph --oneline to visualize all branch tips and merges across the whole repo.",
    "Use git log -n 5 --stat to view high-level file change statistics for the last 5 commits.",
    "Use git log --merges to list only merge commits in the project repository history.",
    "Use git log --no-merges to list only linear commits excluding merge commits from history.",
    "Use git rebase --root -i to interactively rebase the entire project history from the root commit.",
    "Use git rebase --autostash to automatically stash uncommitted changes before rebasing and pop after.",
    "Use git cherry-pick <commit1>..<commit2> to apply a range of consecutive commits onto the current branch.",
    "Use git checkout -b <new_branch> <start_point> to create and switch to a new branch from any commit.",
    "Use git switch -c <new_branch> to use modern git switch syntax to create and switch branches.",
    "Use git switch - to switch back to the previously checked-out git branch quickly.",
    "Use git switch <branch> to switch between existing branches cleanly using modern git syntax.",
    "Use git restore --source=<commit> <file> to restore a file to its state at a specific historical commit.",
    "Use git bundle create repo.bundle HEAD main to package a git repository into a single transferable file.",
    "Use git bundle verify repo.bundle to check that a git bundle file is valid and can be extracted.",
    "Use git shortlog -e -s -n to generate a contributor list with email addresses sorted by commit count.",
    "Use git describe --tags --always to generate human-readable version strings based on the nearest tag.",
    "Use docker ps to view all currently active running Docker containers.",
    "Use docker ps -a to inspect all containers including stopped and exited ones.",
    "Use docker images to view locally cached container images and their sizes.",
    "Use docker build -t <name>:<tag> . to build a container image from a Dockerfile.",
    "Use docker build --no-cache -t <name> . to force rebuilding all Docker image layers.",
    "Use docker run -d -p 8080:80 <image> to run a container detached with port mapping.",
    "Use docker run --rm -it <image> /bin/sh to test a container interactively with auto-cleanup.",
    "Use docker exec -it <container_id> /bin/bash to open an interactive shell inside a container.",
    "Use docker logs -f <container_id> to stream container standard output and error in real time.",
    "Use docker logs --tail 100 <container_id> to view only the last 100 lines of container logs.",
    "Use docker stop <container_id> to gracefully stop a running container with SIGTERM.",
    "Use docker kill <container_id> to forcefully terminate a non-responsive container with SIGKILL.",
    "Use docker rm <container_id> to remove a stopped container and free up disk space.",
    "Use docker rmi <image_id> to delete a locally cached Docker container image.",
    "Use docker system prune -af to remove all unused containers, networks, and images.",
    "Use docker volume ls to list all persistent Docker storage volumes.",
    "Use docker volume create <name> to create named storage volumes for database persistence.",
    "Use docker network ls to view all container bridge, host, and overlay networks.",
    "Use docker stats to monitor real-time CPU, memory, network, and disk I/O per container.",
    "Use docker inspect <container_id> to view full low-level JSON metadata of a container.",
    "Use multi-stage Docker builds to keep final production container images tiny and secure.",
    "Use .dockerignore to exclude target/, .git, and local files from the Docker build context.",
    "Avoid running containers as root; define a dedicated non-root USER in your Dockerfile.",
    "Use specific image tags rather than :latest to ensure deterministic container builds.",
    "Order Dockerfile instructions from least-frequently changing to most-frequently changing.",
    "Combine RUN commands and clean package manager caches in the same layer to minimize size.",
    "Use COPY --chown=user:group to set file ownership during file copy in Dockerfiles.",
    "Use HEALTHCHECK in Dockerfiles to allow container orchestrators to detect unhealthy processes.",
    "Use docker compose up -d to launch multi-container application stacks in the background.",
    "Use docker compose down to stop and clean up multi-container application networks.",
    "Use docker compose logs -f to view synchronized logs across all defined microservices.",
    "Use docker compose ps to view the health and status of services defined in docker-compose.yml.",
    "Use docker compose exec <service> <cmd> to run commands inside a compose service container.",
    "HTTP 200 OK indicates a request succeeded and returned the requested payload.",
    "HTTP 201 Created indicates a request succeeded and created a new resource.",
    "HTTP 204 No Content indicates success with no response payload to return.",
    "HTTP 301 Moved Permanently tells clients to permanently cache the new resource URL.",
    "HTTP 302 Found tells clients to temporarily redirect without updating bookmarks.",
    "HTTP 304 Not Modified tells clients their cached version of a resource is up to date.",
    "HTTP 400 Bad Request indicates malformed request syntax or client validation failure.",
    "HTTP 401 Unauthorized indicates authentication credentials are missing or invalid.",
    "HTTP 403 Forbidden indicates credentials are valid but lack permission for the resource.",
    "HTTP 404 Not Found indicates the requested resource does not exist on the server.",
    "HTTP 405 Method Not Allowed indicates the HTTP verb is unsupported for this route.",
    "HTTP 408 Request Timeout indicates the server timed out waiting for the client request.",
    "HTTP 409 Conflict indicates a state conflict, such as attempting duplicate unique entries.",
    "HTTP 422 Unprocessable Entity indicates well-formed syntax but semantic validation errors.",
    "HTTP 429 Too Many Requests indicates the client has exceeded allowed rate limits.",
    "HTTP 500 Internal Server Error indicates an unhandled server-side exception or panic.",
    "HTTP 502 Bad Gateway indicates an upstream proxy or gateway received an invalid response.",
    "HTTP 503 Service Unavailable indicates the server is temporarily overloaded or down.",
    "HTTP 504 Gateway Timeout indicates an upstream service failed to respond within limits.",
    "Use ETag headers to implement conditional caching and optimistic concurrency control.",
    "Use Cache-Control: max-age=31536000, immutable for static, content-hashed frontend assets.",
    "Use Cache-Control: no-cache to force clients to revalidate cached responses before reuse.",
    "Use Content-Security-Policy (CSP) headers to prevent Cross-Site Scripting (XSS) attacks.",
    "Set Strict-Transport-Security (HSTS) headers to force browsers to use HTTPS exclusively.",
    "Set X-Content-Type-Options: nosniff to prevent browsers from MIME-sniffing response types.",
    "Set X-Frame-Options: DENY or SAMEORIGIN to protect web applications against clickjacking.",
    "Set SameSite=Strict or SameSite=Lax on session cookies to prevent CSRF vulnerabilities.",
    "Always mark sensitive authentication cookies with the HttpOnly flag to block JS access.",
    "Always mark production session cookies with the Secure flag so they are sent over TLS only.",
    "Use CORS headers (Access-Control-Allow-Origin) to restrict which origins can fetch your API.",
    "Avoid Access-Control-Allow-Origin: * when credentials (cookies/auth headers) are allowed.",
    "Use JSON Web Tokens (JWT) for stateless authentication across distributed microservices.",
    "Always verify JWT signature, expiration (exp), and issuer (iss) claims on every request.",
    "Use short-lived access tokens (15 minutes) paired with secure refresh tokens in auth systems.",
    "Hash passwords using Argon2id or bcrypt; never store passwords in plaintext or MD5/SHA-256.",
    "L1 CPU data cache access latency is typically ~1 nanosecond (around 4-5 clock cycles).",
    "L2 CPU cache access latency is typically ~3-4 nanoseconds (around 12-14 clock cycles).",
    "L3 CPU shared cache access latency is typically ~10-20 nanoseconds (around 40-60 cycles).",
    "Main system RAM access latency is typically ~60-80 nanoseconds (hundreds of clock cycles).",
    "A branch misprediction costs approximately 15-20 CPU clock cycles in modern pipelines.",
    "Sequential memory access is orders of magnitude faster than random pointer chasing due to prefetching.",
    "Cache lines in modern x86_64 and ARM CPUs are 64 bytes wide; data is fetched in 64-byte chunks.",
    "False sharing occurs when two threads modify distinct variables located on the same 64-byte cache line.",
    "Align multithreaded atomic variables to 64-byte boundaries to eliminate false sharing contention.",
    "Memory Management Units (MMU) translate virtual memory addresses to physical RAM addresses.",
    "Translation Lookaside Buffers (TLB) cache recent virtual-to-physical address translations in the CPU.",
    "A TLB miss forces the hardware page table walker to perform multi-level memory lookups.",
    "Use Linux HugePages (2MB or 1GB pages) for memory-intensive workloads to drastically reduce TLB misses.",
    "The CPU instruction cache (I-cache) stores decoded machine instructions for fast loop execution.",
    "Keep inner loops small and tight so instructions fit entirely within the fast L1 instruction cache.",
    "Structure-of-Arrays (SoA) layout improves SIMD vectorization over Array-of-Structures (AoS).",
    "Array-of-Structures (AoS) layout is optimal when all fields of an entity are accessed together.",
    "Branchless programming using arithmetic bit-tricks avoids CPU branch prediction penalties.",
    "Modern CPUs use out-of-order execution to run independent instructions while waiting for memory.",
    "Speculative execution predicts branch targets and begins executing before conditions resolve.",
    "CPU memory barriers and fences enforce ordering between memory loads and stores across cores.",
    "Atomic compare-and-swap (CAS) is the fundamental primitive behind lock-free concurrent algorithms.",
    "Lock-free data structures guarantee that at least one thread makes progress in a finite number of steps.",
    "Wait-free data structures guarantee that every thread makes progress in a bounded number of steps.",
    "Write-Ahead Logging (WAL) guarantees database durability (ACID) by logging mutations before flushing pages.",
    "Log-Structured Merge-trees (LSM) optimize write throughput by converting random writes to sequential logs.",
    "B+ Trees optimize read throughput in relational databases with high fanout and shallow tree depth.",
    "Multi-Version Concurrency Control (MVCC) allows readers to read consistent snapshots without blocking writers.",
    "Never hardcode API keys, database credentials, or private certificates in source code repositories.",
    "Use git-secrets or gitleaks in pre-commit hooks to detect accidental secret commits before push.",
    "Rotate compromised credentials immediately and audit access logs for unauthorized activity.",
    "Use environment variables or secure secret managers (Vault, AWS Secrets Manager) for credentials.",
    "Use constant-time comparison functions when comparing cryptographic hashes to prevent timing attacks.",
    "Use cryptographically secure pseudorandom number generators (CSPRNG) for tokens and keys.",
    "Never use Math.random() or rand::random() for cryptographic keys, tokens, or security nonces.",
    "Use TLS 1.3 whenever possible; it eliminates insecure legacy cipher suites and speeds up handshakes.",
    "Use AEAD (Authenticated Encryption with Associated Data) ciphers like AES-256-GCM or ChaCha20-Poly1305.",
    "Never reuse an initialization vector (IV) or nonce with the same cryptographic encryption key.",
    "Sanitize and parameterize all database queries to eliminate SQL injection vulnerabilities completely.",
    "Validate uploaded file types by inspecting magic byte headers, not just client-provided file extensions.",
    "Store uploaded user files in separate object storage (S3) rather than directly on application servers.",
    "Serve user-uploaded files from dedicated, isolated domains to prevent cookie and session theft.",
    "Escape HTML output properly to prevent stored, reflected, and DOM-based Cross-Site Scripting (XSS).",
    "Use parameterized command execution (e.g. Command::args) to prevent shell command injection flaws.",
    "Apply the principle of least privilege: grant processes and API tokens only the permissions they strictly need.",
    "Isolate untrusted tenant workloads using lightweight virtualization or isolated Linux containers.",
    "Keep dependencies updated to minimize exposure to known Common Vulnerabilities and Exposures (CVEs).",
    "Monitor security advisories for your tech stack and patch critical vulnerabilities promptly.",
    "Review code with empathy: critique the implementation and architecture, never the person.",
    "Ask clarifying questions rather than demanding changes when reviewing unfamiliar design decisions.",
    "Verify that new features or bug fixes include corresponding automated unit or regression tests.",
    "Check edge cases during code review: empty collections, null inputs, zero values, and max limits.",
    "Check error paths during code review: verify that failures release locks and clean up open handles.",
    "Ensure logging statements provide actionable context (who, what, why) without leaking user PII.",
    "Check that timeout configurations exist for all outbound HTTP, gRPC, and database connections.",
    "Verify that background threads or async tasks have explicit cancellation or shutdown handles.",
    "Avoid boolean flag arguments in functions (e.g. process(data, true)); use clear descriptive enums instead.",
    "Refactor complex nested conditionals into early guard clauses to reduce cognitive load.",
    "Extract magic numbers and magic strings into named module constants with clear explanatory names.",
    "Keep variable scopes as narrow as possible: declare variables immediately before their first use.",
    "Avoid global mutable state; pass dependencies explicitly via constructor dependency injection.",
    "Write code for the human reader first, and the compiler second: readable code is maintainable code.",
    "Delete commented-out code immediately; git history preserves deleted code if ever needed again.",
    "Use table-driven tests when verifying multiple input/output permutations for the same function.",
    "Ensure tests fail with clear, descriptive assertion failure messages when regressions occur.",
    "Do not test third-party library internals; test your own application's integration with them.",
    "Keep test runtimes fast: slow test suites discourage developers from running them frequently.",
    "Run tests in random order to detect subtle inter-test dependencies and shared global state leaks.",
    "TCP provides reliable, ordered, and error-checked delivery of a stream of bytes between hosts.",
    "TCP three-way handshake: SYN -> SYN-ACK -> ACK establishes a bidirectional network connection.",
    "TCP four-way handshake: FIN -> ACK -> FIN -> ACK closes a bidirectional connection gracefully.",
    "TCP TIME_WAIT state prevents delayed segments from an old connection interfering with a new one.",
    "TCP Nagle algorithm buffers small outgoing packets to improve network bandwidth efficiency.",
    "Set TCP_NODELAY on low-latency sockets to disable the Nagle algorithm and flush packets immediately.",
    "TCP window size advertises how many bytes a receiver can buffer before acknowledging packets.",
    "TCP congestion control algorithms (BBR, CUBIC, Reno) dynamically adjust transmission rates to avoid packet loss.",
    "UDP is a connectionless, lightweight protocol suitable for real-time gaming, audio, and video streaming.",
    "UDP does not provide built-in retransmission, packet ordering, or congestion control mechanisms.",
    "QUIC is a UDP-based transport protocol that eliminates head-of-line blocking across multiplexed streams.",
    "QUIC integrates TLS 1.3 encryption directly into the transport layer for 0-RTT connection resumption.",
    "QUIC supports connection migration: clients can switch between Wi-Fi and cellular without dropping sessions.",
    "HTTP/2 multiplexes multiple concurrent requests and responses over a single underlying TCP connection.",
    "HTTP/2 uses HPACK header compression to significantly reduce HTTP metadata overhead over the wire.",
    "HTTP/2 can suffer from TCP head-of-line blocking if packet loss occurs on the shared TCP connection.",
    "HTTP/3 runs over QUIC/UDP, eliminating TCP head-of-line blocking across independent concurrent streams.",
    "WebSockets provide full-duplex, bidirectional communication channels over a single persistent TCP connection.",
    "A WebSocket connection begins as an HTTP request and upgrades via the HTTP 101 Switching Protocols header.",
    "Use gRPC over HTTP/2 with Protocol Buffers for high-performance, strongly typed RPC microservice communication.",
    "Protocol Buffers serialize structured data into compact binary payloads much smaller and faster than JSON.",
    "DNS translates human-readable domain names into numerical IP addresses using UDP and TCP port 53.",
    "DNS A records map hostnames to IPv4 addresses; AAAA records map hostnames to 128-bit IPv6 addresses.",
    "DNS CNAME records create alias records pointing one domain name to another canonical domain name.",
    "DNS MX records specify the mail exchange servers responsible for accepting email for a domain.",
    "DNS TXT records hold arbitrary text attributes, commonly used for SPF, DKIM, and domain ownership verification.",
    "DNS TTL (Time-To-Live) defines how many seconds recursive resolvers can cache DNS lookup results.",
    "Linux epoll provides O(1) event-driven I/O multiplexing capable of scaling to tens of thousands of sockets.",
    "Use EPOLLET (edge-triggered epoll) with non-blocking sockets for maximum I/O throughput in event loops.",
    "Linux io_uring provides asynchronous submission and completion ring buffers for zero-syscall I/O.",
    "FreeBSD and macOS use kqueue for scalable, kernel-level event notification and file monitoring.",
    "A process context switch saves CPU registers and switches virtual memory page tables via the CR3 register.",
    "Thread context switching within the same process is faster than process switching because page tables are shared.",
    "Preemptive multitasking preempts running threads when their allocated time slice (quantum) expires.",
    "Linux Completely Fair Scheduler (CFS) uses red-black trees to track and balance thread execution runtime.",
    "Hardware interrupts temporarily halt the CPU to execute dedicated interrupt service routines (ISR).",
    "Softirqs and tasklets in Linux defer heavy interrupt processing out of top-half hardware interrupt handlers.",
    "Copy-On-Write (COW) allows fork() to create child processes almost instantaneously by sharing physical RAM pages.",
    "Anonymous memory pages are backed by system swap space rather than filesystem files on disk.",
    "The Linux Out-Of-Memory (OOM) killer terminates processes with high oom_score when physical RAM is exhausted.",
    "Tune /proc/sys/vm/swappiness to control how aggressively the Linux kernel swaps memory pages to disk.",
    "Dirty memory pages are modified RAM pages that have not yet been written back to underlying disk storage.",
    "The sync system call flushes all dirty memory buffers and file caches to disk storage immediately.",
    "Fsync(fd) flushes modified file data and metadata for a specific file descriptor to physical media.",
    "Fdatasync(fd) flushes file data to disk without forcing metadata updates if file size did not change.",
    "Memory-mapped files (mmap) map disk files directly into process virtual memory space for zero-copy access.",
    "Posix_fadvise allows applications to hint to the kernel about expected file access patterns (sequential, random).",
    "Pipe buffers in Linux default to 64KB capacity and block writers when full until readers consume data.",
    "Array / Vector: O(1) random access, O(1) amortized append, O(N) insertion and deletion at arbitrary index.",
    "Singly Linked List: O(1) head insertion and removal, O(N) traversal, poor CPU cache locality.",
    "Doubly Linked List: O(1) bidirectional insertion and removal when holding a node pointer, high pointer overhead.",
    "Hash Table: O(1) average lookup, insert, and delete; O(N) worst-case on heavy hash collisions.",
    "Binary Search Tree: O(log N) average search, insert, and delete; O(N) worst-case if unbalanced.",
    "Red-Black Tree (std::collections::BTreeMap): O(log N) guaranteed worst-case search, insert, and delete.",
    "B-Tree: High fanout, shallow depth, optimized for block storage and CPU cache-line utilization.",
    "Binary Heap: O(1) find-max/min, O(log N) insert and extract, ideal for priority queues.",
    "Trie (Prefix Tree): O(K) lookup where K is key length, optimal for autocomplete and prefix matching.",
    "Radix Tree / Patricia Trie: Space-optimized trie that merges nodes with single children.",
    "Bloom Filter: Space-efficient probabilistic data structure that tests set membership with zero false negatives.",
    "Count-Min Sketch: Probabilistic data structure for estimating frequencies of events in data streams.",
    "HyperLogLog: Probabilistic algorithm for estimating cardinality (unique items) of massive datasets in minimal RAM.",
    "Ring Buffer / Circular Queue: Fixed-size FIFO buffer that overwrites oldest elements without reallocating.",
    "Disjoint-Set / Union-Find: Near O(1) operations with path compression, ideal for Kruskal's algorithm and graph cycles.",
    "Dijkstra's Algorithm: Finds the shortest path in graphs with non-negative edge weights using a priority queue.",
    "A* Search: Informs shortest-path search with heuristics to guide exploration toward the goal node efficiently.",
    "Breadth-First Search (BFS): Explores graph level by level; finds the shortest path in unweighted graphs.",
    "Depth-First Search (DFS): Explores graph branches to full depth; ideal for topological sorting and cycle detection.",
    "Binary Search: Finds elements in sorted arrays in O(log N) time by repeatedly halving the search range.",
    "Two Pointers technique: Solves subarray, palindrome, and sorted pair problems in O(N) time with O(1) space.",
    "Sliding Window technique: Computes statistics over continuous subarrays or substrings in O(N) time.",
    "Fast and Slow Pointers (Floyd's Cycle Finding): Detects loops in linked lists using O(1) auxiliary space.",
    "Dynamic Programming: Solves complex problems by breaking them into overlapping subproblems with memoization.",
    "Greedy Algorithms: Make the locally optimal choice at each step; optimal for Huffman coding and minimum spanning trees.",
    "Topological Sort: Orders vertices in a Directed Acyclic Graph (DAG) such that every directed edge goes forward.",
    "Use GitHub Actions matrix strategy to test your code across Linux, macOS, and Windows in parallel.",
    "Use actions/checkout@v4 with fetch-depth: 0 if your build or changelog tooling requires full git history.",
    "Use actions/cache@v4 to cache Cargo target/ directories and cargo registry caches between CI workflow runs.",
    "Use dtolnay/rust-toolchain action in GitHub Actions to install pinned Rust compiler toolchains reliably.",
    "Pin GitHub Actions to immutable full commit SHAs rather than mutable tags for maximum supply-chain security.",
    "Run cargo clippy and cargo fmt checks in separate CI jobs to provide fast, independent lint feedback.",
    "Use pull_request: paths: filters in GitHub Actions to trigger workflows only when relevant files change.",
    "Set workflow permissions to read-all by default in GitHub Actions and grant write permissions explicitly.",
    "Use GitHub Environment Secrets with required reviewers for production deployment workflows.",
    "Use concurrency groups (cancel-in-progress: true) in GitHub Actions to cancel obsolete CI runs on new commits.",
    "In Makefiles, declare non-file targets with .PHONY: target to prevent collisions with existing filenames.",
    "In Makefiles, prefix recipe commands with @ to silence echoing the command to stdout during execution.",
    "In Makefiles, use $(MAKE) rather than hardcoded 'make' to invoke recursive sub-make invocations cleanly.",
    "Use cargo-release or release-please to automate version bumping, changelog updates, and git tagging.",
    "Use semantic-release to automatically determine SemVer increments based on conventional commit messages.",
    "Generate Software Bill of Materials (SBOM) for container images and binaries using syft or cargo-auditable.",
    "Sign release binaries and container images cryptographically with cosign to establish provenance.",
    "Use cargo-dist to generate multi-platform release binaries, installers, and GitHub Releases automatically.",
    "The Four Golden Signals of monitoring: Latency, Traffic, Errors, and Saturation.",
    "Measure latency with percentiles (p50, p90, p99, p99.9) rather than averages, which conceal extreme outliers.",
    "SLI (Service Level Indicator): A carefully defined quantitative measure of service performance.",
    "SLO (Service Level Objective): A target value or range for a service level measured by an SLI.",
    "SLA (Service Level Agreement): A formal agreement with users defining consequences if SLOs are breached.",
    "Error Budget: The fraction of time a service is allowed to be down or out of spec within an SLO window.",
    "Use OpenTelemetry (OTel) for standardized, vendor-neutral traces, metrics, and logs instrumentation.",
    "Sample distributed traces adaptively: record 100% of error traces while sampling high-volume success traces.",
    "Expose application metrics in Prometheus exposition format on a dedicated /metrics endpoint.",
    "Use Prometheus Counter metrics for monotonically increasing values (requests processed, errors encountered).",
    "Use Prometheus Gauge metrics for values that go up and down (current memory usage, active connection count).",
    "Use Prometheus Histogram metrics to track distributions of values like request latencies and response sizes.",
    "Practice Chaos Engineering by injecting controlled faults (latency, network partitions, kills) into staging.",
    "Run blameless postmortems after production incidents to focus on systemic process and tooling improvements.",
    "Maintain runbooks with step-by-step diagnostic and recovery commands for on-call engineers during outages.",
    "Use From or Into when converting from u8 to u16 for lossless numeric widening.",
    "Use From or Into when converting from u16 to u32 for lossless integer widening.",
    "Use From or Into when converting from u32 to u64 for widening unsigned integers without risk of overflow.",
    "Use From or Into when converting from u64 to u128 for expanding integer width losslessly.",
    "Use From or Into when converting from i8 to i16 for widening signed numbers safely.",
    "Use From or Into when converting from i16 to i32 for lossless signed integer promotion.",
    "Use From or Into when converting from i32 to i64 for expanding signed integers without data loss.",
    "Use From or Into when converting from i64 to i128 for preventing signed integer overflows.",
    "Use From or Into when converting from f32 to f64 for expanding float precision without rounding loss.",
    "Use From or Into when converting from &str to String for converting a borrowed string slice to an owned heap String.",
    "Use From or Into when converting from &[u8] to Vec<u8> for creating an owned byte buffer from a borrowed slice.",
    "Use From or Into when converting from &Path to PathBuf for creating an owned path buffer from a borrowed path slice.",
    "Use From or Into when converting from &OsStr to OsString for creating an owned OS string from a borrowed OS slice.",
    "Use From or Into when converting from Vec<T> to Box<[T]> for freeing spare vector capacity to save memory.",
    "Use From or Into when converting from String to Box<str> for storing an owned immutable string without extra capacity overhead.",
    "Use From or Into when converting from Box<T> to Pin<Box<T>> for pinning a heap-allocated struct to guarantee its address cannot change.",
    "Use Iterator::find to search for the first element satisfying a predicate function in Rust.",
    "Use Iterator::position to find the zero-based index of the first matching element in Rust.",
    "Use Iterator::rfind to search backwards for the last element matching a predicate in Rust.",
    "Use Iterator::max to find the maximum element in an iterator of Ord items in Rust.",
    "Use Iterator::min to find the minimum element in an iterator of Ord items in Rust.",
    "Use Iterator::max_by_key to find the element that maximizes a custom key extraction function in Rust.",
    "Use Iterator::min_by_key to find the element that minimizes a custom key extraction function in Rust.",
    "Use Iterator::count to consume an iterator and count how many items it yielded in Rust.",
    "Use Iterator::last to consume an iterator and return only its final element in Rust.",
    "Use Iterator::nth to skip n elements and return the subsequent item in O(n) in Rust.",
    "Use Iterator::cycle to create an infinite iterator that repeats elements continuously in Rust.",
    "Use Iterator::cloned to convert an iterator of references into an iterator of cloned values in Rust.",
    "Use Iterator::copied to convert an iterator of references into an iterator of copied values in Rust.",
    "Use Iterator::fuse to guarantee that an iterator returns None forever after the first None in Rust.",
    "Use Iterator::inspect to perform side effects like logging on each iterated element in Rust.",
    "Use Iterator::partition to split an iterator into two collections based on a boolean predicate in Rust.",
    "Use Iterator::unzip to split an iterator of pairs into two separate collections in Rust.",
    "Use Iterator::cmp to compare two iterators lexicographically based on their element order in Rust.",
    "Use Iterator::eq to check if two iterators yield identical elements in the same order in Rust.",
    "Use Iterator::ne to check if two iterators differ in yielded elements or length in Rust.",
    "Use Iterator::lt to test if one iterator is lexicographically less than another in Rust.",
    "Use Iterator::gt to test if one iterator is lexicographically greater than another in Rust.",
    "Use Iterator::ge to test if an iterator is lexicographically greater than or equal to another in Rust.",
    "Use Iterator::le to test if an iterator is lexicographically less than or equal to another in Rust.",
    "Use curl -sSL to download files silently while following HTTP redirects.",
    "Use curl -m 30 to set a strict 30-second maximum execution timeout on HTTP transfers.",
    "Use curl --retry 3 to retry transient HTTP network errors up to 3 times automatically.",
    "Use curl -u user:token to pass HTTP basic authentication credentials.",
    "Use curl -H 'Accept: application/json' to request JSON representation from REST APIs.",
    "Use git log -p -2 to view the patch diffs of the last 2 commits.",
    "Use git diff -w to view git diffs ignoring all whitespace changes.",
    "Use git diff --name-only to list only the names of modified files without diff bodies.",
    "Use git status -uall to display individual untracked files within untracked directories.",
    "Use git show-branch to inspect commit summaries across multiple branches side by side.",
    "Use git grep -n to search for text patterns within git-tracked files with line numbers.",
    "Use git archive --format=tar.gz -o release.tar.gz HEAD to export the repository commit as a tarball.",
    "Use rg --stats to display detailed search statistics including files searched and matches.",
    "Use rg -c to count the total number of matching lines per file.",
    "Use rg -l to print only the paths of files containing at least one match.",
    "Use rg --null to output a zero byte after file names for safe xargs -0 piping.",
    "Use fd -0 to separate file paths with null bytes for safe xargs -0 integration.",
    "Use fd -s to perform case-sensitive file search instead of smart-case.",
    "Use fd -I to include files that match patterns in .gitignore files.",
    "Use tar -tzvf archive.tar.gz to list the contents of a compressed tar archive without extracting it.",
    "Use tar -C /dest -xzvf archive.tar.gz to extract a tar archive into a specific destination directory.",
    "Use rsync -avz --delete src/ dest/ to mirror a source directory to destination, deleting removed files.",
    "Use rsync -P to show transfer progress and allow resuming interrupted file copies.",
    "Use rsync -n (or --dry-run) to preview file synchronization actions without making changes.",
    "Use sed -n '10,20p' <file> to print lines 10 through 20 of a text file.",
    "Use sed '/pattern/d' <file> to delete all lines matching a pattern from text output.",
    "Use awk NF > 0 to filter out completely empty lines from a text stream.",
    "Use awk '!seen[$0]++' to remove duplicate lines from an unsorted file while preserving order.",
    "Use sort -k1,1 -k2,2n to sort by primary string column and secondary numeric column.",
    "Use uniq -d to print only duplicate lines that appear consecutively in a sorted stream.",
    "Use uniq -u to print only unique lines that appear exactly once in a sorted stream.",
    "Use cut -c1-10 to extract the first 10 characters of each line in a file.",
    "Use split -b 100M largefile part_ to split a large file into 100MB chunks.",
    "Use cat part_* > reconstructed to recombine split file chunks back into the original file.",
    "Use find -mmin -60 to find files modified within the last 60 minutes.",
    "Use find -size +100M to locate files larger than 100 megabytes across a filesystem.",
    "Use find -empty to locate empty files and directories for cleanup.",
    "Use xargs -0 to process null-delimited input safely to handle filenames with spaces.",
    "Use xargs -n 1 to pass input items to command invocations one item at a time.",
    "In systems programming, monotonic clocks (std::time::Instant) is used to measure duration intervals reliably even if the system wall-clock changes.",
    "In systems programming, system wall clocks (std::time::SystemTime) is used to record timestamps for calendar dates and persistent records.",
    "In systems programming, atomic load-linked / store-conditional (LL/SC) is used to implement atomic read-modify-write sequences on RISC and ARM CPUs.",
    "In systems programming, memory fences is used to prevent CPU hardware reordering of memory reads and writes across cores.",
    "In systems programming, SIMD lane execution is used to execute identical arithmetic instructions across multiple data elements in parallel.",
    "In systems programming, vector register width (256/512 bits) is used to allow processing 8 to 16 floating-point values in a single clock cycle.",
    "In systems programming, stack allocation is used to allocate local variables with zero runtime overhead via pointer bumping.",
    "In systems programming, heap allocation is used to allocate dynamically sized memory that outlives the current stack frame.",
    "In systems programming, memory fragmentation is used to occurs when free heap memory is split into non-contiguous small chunks.",
    "In systems programming, jemalloc allocator is used to mitigate heap fragmentation and improve multithreaded allocation scalability.",
    "In systems programming, mimalloc allocator is used to provide high-throughput, low-latency heap allocations for concurrent workloads.",
    "In systems programming, stack overflow is used to happens when recursive function calls exceed the allocated thread stack space.",
    "In systems programming, thread stack guards is used to trigger page faults on stack overflow to prevent silent memory corruption.",
    "In systems programming, core dumps is used to capture full process memory images upon crash for offline post-mortem debugging.",
    "In systems programming, dwarf debug information is used to map compiled machine instructions back to source code files and line numbers.",
    "In systems programming, symbol stripping is used to remove debug symbol tables from release binaries to reduce download sizes.",
    "In systems programming, PIE (Position Independent Executable) is used to enable Address Space Layout Randomization (ASLR) for security.",
    "In systems programming, ASLR (Address Space Layout Randomization) is used to randomize memory layout to prevent buffer-overflow exploit attacks.",
    "In systems programming, DEP / NX bit is used to mark memory pages as non-executable to prevent executing code in data segments.",
    "In systems programming, stack canaries is used to detect stack buffer overflows before returning from vulnerable functions.",
    "In systems programming, relro (Relocation Read-Only) is used to make ELF binary relocation sections read-only to protect against GOT overwrites.",
    "Use -C opt-level=3 to enable aggressive compiler optimization passes in rustc.",
    "Use -C target-cpu=native to enable all instruction set extensions supported by the host CPU.",
    "Use -C link-arg=-s to pass strip flags directly to the system linker during binary compilation.",
    "Use -C overflow-checks=on to enable integer overflow checking even in optimized release builds.",
    "Use -C panic=abort to disable unwinding tables to make binaries smaller and panics faster.",
    "Use -C debuginfo=1 to include line table debug information without variable inspection bloat.",
    "Use -C lto=thin to enable thin Link Time Optimization for balanced compile time and optimization.",
    "Use -C codegen-units=1 to reduce parallel codegen units to single-threaded to maximize optimization.",
    "Use -C prefer-dynamic to prefer linking dynamically to shared standard library objects.",
    "Use #[track_caller] to attribute panic locations to the function caller rather than function definition.",
    "Use #[must_use = '...'] to provide a custom explanatory compiler warning if a return value is discarded.",
    "Use #[inline(never)] to prevent the compiler from inlining large or rarely executed functions.",
    "Use #[cold] to hint to the compiler optimizer that a code branch is unlikely to be executed.",
    "Use #[deprecated(since = '...', note = '...'] to mark functions as deprecated with replacement instructions.",
    "Use #[allow(dead_code)] to silence dead code warnings during active exploratory feature development.",
    "Use #[warn(clippy::all)] to enable all clippy warning lints explicitly on a specific module.",
    "Use #[deny(missing_docs)] to enforce that all public items in a crate have documentation comments.",
    "Use concat!(\"a\", \"b\") to concatenate string literals at compile time into a single 'static str.",
    "Use stringify!(expression) to convert any Rust token tree into its string representation at compile time.",
    "Use env!(\"CARGO_PKG_VERSION\") to read environment variables at compile time as 'static str literals.",
    "Use option_env!(\"VAR\") to read optional environment variables at compile time as Option<&'static str>.",
    "Use include_str!(\"path\") to embed the text contents of a file directly into the binary at compile time.",
    "Use include_bytes!(\"path\") to embed binary file bytes into the compiled executable as a &'static [u8].",
    "Use cfg!(target_os = \"linux\") to evaluate boolean target configuration expressions at compile time.",
    "Use compile_error!(\"msg\") to halt compilation with a descriptive custom compiler error message.",
    "Use /proc/version to inspect the exact Linux kernel version, compiler build, and build timestamp.",
    "Use /proc/uptime to view system uptime in seconds and idle time across all CPU cores.",
    "Use /proc/diskstats to inspect raw disk read/write operation counts and sector counts.",
    "Use /proc/net/dev to inspect network interface packet counters, bytes received, and errors.",
    "Use /proc/net/tcp to view raw kernel socket table entries for all active TCP connections.",
    "Use /proc/sys/fs/file-nr to check allocated, free, and maximum file descriptors system-wide.",
    "Use /proc/sys/kernel/pid_max to view or set the maximum PID value allowed by the Linux kernel.",
    "Use /proc/sys/kernel/hostname to read or update the system hostname directly from the sysctl interface.",
    "Use /sys/devices/system/cpu/ to inspect CPU scaling governors, frequencies, and topology cores.",
    "Use /sys/class/net/ to inspect network interface queue statistics and link state flags.",
    "Use /sys/class/block/ to inspect block device scheduler algorithms and rotational flags.",
    "Use /sys/kernel/debug/ to access debugfs for deep kernel tracing and subsystem diagnostics.",
    "Use ${var:-default} to use a default fallback value if a variable is unset or empty.",
    "Use ${var:=default} to assign and use a default fallback value if a variable is unset.",
    "Use ${var:+alternate} to use an alternate value only if a variable is set and non-empty.",
    "Use ${var:?error_msg} to exit with an error message if a variable is unset or empty.",
    "Use ${#var} to calculate the length in characters of a string stored in a variable.",
    "Use ${var%suffix} to remove the shortest matching trailing suffix from a variable string.",
    "Use ${var%%suffix} to remove the longest matching trailing suffix from a variable string.",
    "Use ${var#prefix} to remove the shortest matching leading prefix from a variable string.",
    "Use ${var##prefix} to remove the longest matching leading prefix from a variable string.",
    "Use ${var/pattern/repl} to replace the first match of a pattern inside a variable string.",
    "Use ${var//pattern/repl} to replace all occurrences of a pattern inside a variable string.",
    "Refactor network handlers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor parser logic into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor database queries into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor cache managers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor configuration loaders into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor CLI command parsers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor file storage helpers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor authentication filters into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor event dispatchers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor background worker tasks into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor serialization routines into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor stream pipelines into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor encryption utilities into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor token bucket rate limiters into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor metrics collectors into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor logging subscribers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor audit log writers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor heartbeat monitors into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor session validators into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor error recovery branches into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor diff generators into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor markdown formatters into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor JSON reparsers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor connection poolers into smaller, cohesive helper functions to improve testability and readability.",
    "Refactor system signal listeners into smaller, cohesive helper functions to improve testability and readability.",
    "Benchmark network handlers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark parser logic under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark database queries under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark cache managers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark configuration loaders under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark CLI command parsers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark file storage helpers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark authentication filters under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark event dispatchers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark background worker tasks under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark serialization routines under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark stream pipelines under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark encryption utilities under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark token bucket rate limiters under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark metrics collectors under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark logging subscribers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark audit log writers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark heartbeat monitors under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark session validators under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark error recovery branches under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark diff generators under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark markdown formatters under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark JSON reparsers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark connection poolers under realistic load before optimizing algorithms or caching strategies.",
    "Benchmark system signal listeners under realistic load before optimizing algorithms or caching strategies.",
    "Document network handlers the non-obvious design constraints and invariants in code comments.",
    "Document parser logic the non-obvious design constraints and invariants in code comments.",
    "Document database queries the non-obvious design constraints and invariants in code comments.",
    "Document cache managers the non-obvious design constraints and invariants in code comments.",
    "Document configuration loaders the non-obvious design constraints and invariants in code comments.",
    "Document CLI command parsers the non-obvious design constraints and invariants in code comments.",
    "Document file storage helpers the non-obvious design constraints and invariants in code comments.",
    "Document authentication filters the non-obvious design constraints and invariants in code comments.",
    "Document event dispatchers the non-obvious design constraints and invariants in code comments.",
    "Document background worker tasks the non-obvious design constraints and invariants in code comments.",
    "Document serialization routines the non-obvious design constraints and invariants in code comments.",
    "Document stream pipelines the non-obvious design constraints and invariants in code comments.",
    "Document encryption utilities the non-obvious design constraints and invariants in code comments.",
    "Document token bucket rate limiters the non-obvious design constraints and invariants in code comments.",
    "Document metrics collectors the non-obvious design constraints and invariants in code comments.",
    "Document logging subscribers the non-obvious design constraints and invariants in code comments.",
    "Document audit log writers the non-obvious design constraints and invariants in code comments.",
    "Document heartbeat monitors the non-obvious design constraints and invariants in code comments.",
    "Document session validators the non-obvious design constraints and invariants in code comments.",
    "Document error recovery branches the non-obvious design constraints and invariants in code comments.",
    "Document diff generators the non-obvious design constraints and invariants in code comments.",
    "Document markdown formatters the non-obvious design constraints and invariants in code comments.",
    "Document JSON reparsers the non-obvious design constraints and invariants in code comments.",
    "Document connection poolers the non-obvious design constraints and invariants in code comments.",
    "Document system signal listeners the non-obvious design constraints and invariants in code comments.",
    "Test network handlers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test parser logic edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test database queries edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test cache managers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test configuration loaders edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test CLI command parsers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test file storage helpers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test authentication filters edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test event dispatchers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test background worker tasks edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test serialization routines edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test stream pipelines edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test encryption utilities edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test token bucket rate limiters edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test metrics collectors edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test logging subscribers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test audit log writers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test heartbeat monitors edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test session validators edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test error recovery branches edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test diff generators edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test markdown formatters edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test JSON reparsers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test connection poolers edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Test system signal listeners edge-case boundary values like empty inputs, single items, and maximum capacity.",
    "Profile network handlers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile parser logic memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile database queries memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile cache managers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile configuration loaders memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile CLI command parsers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile file storage helpers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile authentication filters memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile event dispatchers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile background worker tasks memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile serialization routines memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile stream pipelines memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile encryption utilities memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile token bucket rate limiters memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile metrics collectors memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile logging subscribers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile audit log writers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile heartbeat monitors memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile session validators memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile error recovery branches memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile diff generators memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile markdown formatters memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile JSON reparsers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile connection poolers memory allocations using heap profilers to eliminate excessive heap copies.",
    "Profile system signal listeners memory allocations using heap profilers to eliminate excessive heap copies.",
    "Validate network handlers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate parser logic untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate database queries untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate cache managers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate configuration loaders untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate CLI command parsers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate file storage helpers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate authentication filters untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate event dispatchers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate background worker tasks untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate serialization routines untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate stream pipelines untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate encryption utilities untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate token bucket rate limiters untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate metrics collectors untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate logging subscribers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate audit log writers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate heartbeat monitors untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate session validators untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate error recovery branches untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate diff generators untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate markdown formatters untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate JSON reparsers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate connection poolers untrusted inputs at the API boundary before passing them to internal modules.",
    "Validate system signal listeners untrusted inputs at the API boundary before passing them to internal modules.",
    "Isolate network handlers stateful I/O operations from pure mathematical business calculations.",
    "Isolate parser logic stateful I/O operations from pure mathematical business calculations.",
    "Isolate database queries stateful I/O operations from pure mathematical business calculations.",
    "Isolate cache managers stateful I/O operations from pure mathematical business calculations.",
    "Isolate configuration loaders stateful I/O operations from pure mathematical business calculations.",
    "Isolate CLI command parsers stateful I/O operations from pure mathematical business calculations.",
    "Isolate file storage helpers stateful I/O operations from pure mathematical business calculations.",
    "Isolate authentication filters stateful I/O operations from pure mathematical business calculations.",
    "Isolate event dispatchers stateful I/O operations from pure mathematical business calculations.",
    "Isolate background worker tasks stateful I/O operations from pure mathematical business calculations.",
    "Isolate serialization routines stateful I/O operations from pure mathematical business calculations.",
    "Isolate stream pipelines stateful I/O operations from pure mathematical business calculations.",
    "Isolate encryption utilities stateful I/O operations from pure mathematical business calculations.",
    "Isolate token bucket rate limiters stateful I/O operations from pure mathematical business calculations.",
    "Isolate metrics collectors stateful I/O operations from pure mathematical business calculations.",
    "Isolate logging subscribers stateful I/O operations from pure mathematical business calculations.",
    "Isolate audit log writers stateful I/O operations from pure mathematical business calculations.",
    "Isolate heartbeat monitors stateful I/O operations from pure mathematical business calculations.",
    "Isolate session validators stateful I/O operations from pure mathematical business calculations.",
    "Isolate error recovery branches stateful I/O operations from pure mathematical business calculations.",
    "Isolate diff generators stateful I/O operations from pure mathematical business calculations.",
    "Isolate markdown formatters stateful I/O operations from pure mathematical business calculations.",
    "Isolate JSON reparsers stateful I/O operations from pure mathematical business calculations.",
    "Isolate connection poolers stateful I/O operations from pure mathematical business calculations.",
    "Isolate system signal listeners stateful I/O operations from pure mathematical business calculations.",
    "Avoid network handlers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid parser logic sharing mutable state across threads without proper synchronization primitives.",
    "Avoid database queries sharing mutable state across threads without proper synchronization primitives.",
    "Avoid cache managers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid configuration loaders sharing mutable state across threads without proper synchronization primitives.",
    "Avoid CLI command parsers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid file storage helpers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid authentication filters sharing mutable state across threads without proper synchronization primitives.",
    "Avoid event dispatchers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid background worker tasks sharing mutable state across threads without proper synchronization primitives.",
    "Avoid serialization routines sharing mutable state across threads without proper synchronization primitives.",
    "Avoid stream pipelines sharing mutable state across threads without proper synchronization primitives.",
    "Avoid encryption utilities sharing mutable state across threads without proper synchronization primitives.",
    "Avoid token bucket rate limiters sharing mutable state across threads without proper synchronization primitives.",
    "Avoid metrics collectors sharing mutable state across threads without proper synchronization primitives.",
    "Avoid logging subscribers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid audit log writers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid heartbeat monitors sharing mutable state across threads without proper synchronization primitives.",
    "Avoid session validators sharing mutable state across threads without proper synchronization primitives.",
    "Avoid error recovery branches sharing mutable state across threads without proper synchronization primitives.",
    "Avoid diff generators sharing mutable state across threads without proper synchronization primitives.",
    "Avoid markdown formatters sharing mutable state across threads without proper synchronization primitives.",
    "Avoid JSON reparsers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid connection poolers sharing mutable state across threads without proper synchronization primitives.",
    "Avoid system signal listeners sharing mutable state across threads without proper synchronization primitives.",
    "Ensure network handlers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure parser logic background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure database queries background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure cache managers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure configuration loaders background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure CLI command parsers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure file storage helpers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure authentication filters background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure event dispatchers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure background worker tasks background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure serialization routines background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure stream pipelines background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure encryption utilities background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure token bucket rate limiters background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure metrics collectors background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure logging subscribers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure audit log writers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure heartbeat monitors background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure session validators background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure error recovery branches background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure diff generators background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure markdown formatters background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure JSON reparsers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure connection poolers background async tasks handle cancellation gracefully without leaking resources.",
    "Ensure system signal listeners background async tasks handle cancellation gracefully without leaking resources.",
    "Encapsulate network handlers raw struct fields with public getter methods to protect internal invariants.",
    "Encapsulate parser logic raw struct fields with public getter methods to protect internal invariants.",
];

/// Pick a tip from the pool given a pseudo-random seed or tick counter.
pub fn get_tip(seed: usize) -> &'static str {
    TIPS_POOL[seed % TIPS_POOL.len()]
}

/// Pick a random tip using system time with a splitmix64 scramble.
pub fn random_tip() -> &'static str {
    use std::time::{SystemTime, UNIX_EPOCH};
    let nanos = SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .map(|d| d.as_nanos() as u64)
        .unwrap_or(0);
    let mut z = nanos.wrapping_add(0x9e3779b97f4a7c15);
    z = (z ^ (z >> 30)).wrapping_mul(0xbf58476d1ce4e5b9);
    z = (z ^ (z >> 27)).wrapping_mul(0x94d049bb133111eb);
    let hash = z ^ (z >> 31);
    get_tip(hash as usize)
}

fn next_rand(seed: &mut u64) -> u64 {
    *seed = seed.wrapping_mul(6364136223846793005).wrapping_add(1442695040888963407);
    *seed
}

fn shuffle_deck(deck: &mut [usize], seed: &mut u64) {
    for i in (1..deck.len()).rev() {
        let j = (next_rand(seed) as usize) % (i + 1);
        deck.swap(i, j);
    }
}

/// Lifecycle phases for the animated tip typewriter.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TipPhase {
    /// Progressively revealing characters with active cursor.
    Typing,
    /// Holding full tip text with blinking cursor for 10 seconds.
    Holding,
    /// Progressively deleting characters with active cursor.
    Erasing,
    /// Brief pause before next tip starts typing.
    Pause,
}

/// Typewriter animator for rotating developer tips in the footer.
#[derive(Debug, Clone)]
pub struct TipAnimator {
    pub tip_text: &'static str,
    pub char_count: usize,
    pub total_chars: usize,
    pub phase: TipPhase,
    pub hold_ticks: usize,
    pub pause_ticks: usize,
    pub deck: Vec<usize>,
    pub deck_idx: usize,
    pub seed: u64,
}

impl Default for TipAnimator {
    fn default() -> Self {
        Self::new()
    }
}

impl TipAnimator {
    pub fn new() -> Self {
        use std::time::{SystemTime, UNIX_EPOCH};
        let nanos = SystemTime::now()
            .duration_since(UNIX_EPOCH)
            .map(|d| d.as_nanos() as u64)
            .unwrap_or(0x9e3779b97f4a7c15);
        let mut seed = nanos ^ 0xbf58476d1ce4e5b9;

        let mut deck: Vec<usize> = (0..TIPS_POOL.len()).collect();
        shuffle_deck(&mut deck, &mut seed);

        let initial_idx = deck[0];
        let tip_text = TIPS_POOL[initial_idx];
        let total_chars = tip_text.chars().count();

        Self {
            tip_text,
            char_count: 0,
            total_chars,
            phase: TipPhase::Typing,
            hold_ticks: 0,
            pause_ticks: 0,
            deck,
            deck_idx: 0,
            seed,
        }
    }

    /// Advance typewriter animation state by one tick (80ms).
    pub fn tick(&mut self) {
        match self.phase {
            TipPhase::Typing => {
                // Type ultra-fast (4x faster): 8 characters per 80ms tick (~100 chars/sec)
                self.char_count = (self.char_count + 8).min(self.total_chars);
                if self.char_count >= self.total_chars {
                    self.phase = TipPhase::Holding;
                    self.hold_ticks = 125; // 10s hold
                }
            }
            TipPhase::Holding => {
                if self.hold_ticks > 0 {
                    self.hold_ticks -= 1;
                } else {
                    self.phase = TipPhase::Erasing;
                }
            }
            TipPhase::Erasing => {
                // Erase ultra-fast (4x faster): 12 characters per 80ms tick (~150 chars/sec)
                self.char_count = self.char_count.saturating_sub(12);
                if self.char_count == 0 {
                    self.phase = TipPhase::Pause;
                    self.pause_ticks = 4;
                }
            }
            TipPhase::Pause => {
                if self.pause_ticks > 0 {
                    self.pause_ticks -= 1;
                } else {
                    self.deck_idx += 1;
                    if self.deck_idx >= self.deck.len() {
                        self.deck_idx = 0;
                        shuffle_deck(&mut self.deck, &mut self.seed);
                        if self.deck.len() > 1 && TIPS_POOL[self.deck[0]] == self.tip_text {
                            self.deck.swap(0, 1);
                        }
                    }
                    let next = TIPS_POOL[self.deck[self.deck_idx]];
                    self.tip_text = next;
                    self.total_chars = next.chars().count();
                    self.char_count = 0;
                    self.phase = TipPhase::Typing;
                }
            }
        }
    }

    /// Returns currently visible substring and cursor visibility.
    pub fn render_state(&self, tick_n: usize) -> (String, bool) {
        let visible: String = self.tip_text.chars().take(self.char_count).collect();
        let caret = match self.phase {
            TipPhase::Typing | TipPhase::Erasing => true,
            TipPhase::Holding | TipPhase::Pause => (tick_n / 6).is_multiple_of(2),
        };
        (visible, caret)
    }

    /// Renders the animated tip text with blinking cursor for terminal display.
    pub fn render_line(&self, tick_n: usize, max_w: usize) -> String {
        let (visible, caret_on) = self.render_state(tick_n);
        let clipped = if max_w > 1 {
            let limit = max_w.saturating_sub(1);
            if visible.chars().count() > limit {
                visible.chars().take(limit).collect::<String>()
            } else {
                visible
            }
        } else {
            visible
        };
        let caret = if caret_on {
            "\x1b[1;38;2;225;175;95m▌\x1b[0m"
        } else {
            " "
        };
        format!("\x1b[38;2;175;170;160m{clipped}\x1b[0m{caret}")
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_tips_pool_has_exact_2000_items() {
        assert_eq!(TIPS_POOL.len(), 2000);
        let mut seen = std::collections::HashSet::new();
        for tip in TIPS_POOL.iter() {
            assert!(!tip.is_empty(), "Tip string must not be empty");
            assert!(seen.insert(*tip), "Duplicate tip found in pool: {}", tip);
        }
        assert!(!get_tip(0).is_empty());
        assert!(!random_tip().is_empty());
    }

    #[test]
    fn test_tip_animator_cycle() {
        let mut anim = TipAnimator::new();
        assert_eq!(anim.phase, TipPhase::Typing);
        assert_eq!(anim.char_count, 0);

        // Advance typing phase
        for _ in 0..anim.total_chars {
            if anim.phase != TipPhase::Typing {
                break;
            }
            anim.tick();
        }
        assert_eq!(anim.phase, TipPhase::Holding);
        assert_eq!(anim.char_count, anim.total_chars);

        // Advance holding phase (125 ticks)
        for _ in 0..126 {
            anim.tick();
        }
        assert_eq!(anim.phase, TipPhase::Erasing);

        // Advance erasing phase
        for _ in 0..anim.total_chars {
            if anim.phase != TipPhase::Erasing {
                break;
            }
            anim.tick();
        }
        assert_eq!(anim.phase, TipPhase::Pause);

        // Advance pause
        for _ in 0..5 {
            anim.tick();
        }
        assert_eq!(anim.phase, TipPhase::Typing);

        let rendered = anim.render_line(0, 80);
        assert!(!rendered.is_empty());
    }

    #[test]
    fn test_shuffled_deck_uniqueness() {
        let mut anim = TipAnimator::new();
        let mut seen = std::collections::HashSet::new();
        seen.insert(anim.tip_text);

        for _ in 0..100 {
            anim.phase = TipPhase::Pause;
            anim.pause_ticks = 0;
            anim.tick();
            assert!(seen.insert(anim.tip_text), "Repeated tip found within first 100 deck items: {}", anim.tip_text);
        }
        assert_eq!(seen.len(), 101);
    }
}
