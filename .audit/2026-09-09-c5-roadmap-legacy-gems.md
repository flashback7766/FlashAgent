# 2026-09-09 — C5: фиксация вехи допиливаний (Legacy Gems Port) из ~/flashgent

### Status: PASS

### Decision: C5 (добавление вехи в ROADMAP.md по прямому указанию владельца).
Включены согласованные фишки из легаси-версии:
1. Argument Aliasing в flashagent-tools (всеядность названий параметров для локальных LLM).
2. AST Project Outline: фоновый индексатор символов (классы, функции, структуры, трейты) на Rust (`ignore` + regex).
3. Context Steering: канал директив пользователя в `AgentLoop` во время стриминга/цикла без сброса задачи.
4. FileSnapshots & Workspace Rewind: таблица `file_snapshots` в SQLite (`content_before`) для пошагового отката файлов на диске и усечения истории.
5. In-place продолжение (`continueResponse`): дожатие оборванного ответа в той же карточке ассистента без дублирования сообщений.
6. Глубокая защита от инъекций (`untrusted`): одноразовый `nonce` на сессию + зачистка управляющих токенов шаблонов (`neutralise`) + сигнатурная детекция попыток угона.
7. Пакетное редактирование (`multi-file edits`): расширение `edit_file` на массив файлов `files: [{ path, edits }]`.
8. Автоименование сессии: фоновый микро-запрос к модели после первого ответа (не блокирует рабочий цикл и работает даже при выключенной параллелизации тулов).
9. UX-метки для UI-трека: скользящий спидометр скорости `tg_3s` (окно 3 сек) и командная палитра `Cmd/Ctrl+K`.

Files touched:
- ROADMAP.md (добавлена веха C5 со списком согласованных фишек)

Verification:
- `cargo test --workspace` → 92 passed, 0 failed (exit code: 0)
- `cargo clippy --workspace -- -D warnings` → 0 warnings (exit code: 0)

Open questions:
- Порядок реализации: реализовывать C5 после закрытия основных вех ядра/UI или брать точечные улучшения тулов (Argument Aliasing) по ходу работы над соответствующими слоями.

Handoff:
- Возврат к основному плану ROADMAP.md: следующая веха по плану ядра — A9 (MCP клиент, менеджер, реестр) либо переход к треку UI (B1 M3 Expressive kit).
