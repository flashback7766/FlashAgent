# Session Audit: End-to-End Engineering Audit and Repair (b233)

### Status: PASS
Decision: AUDIT-E2E (cross-track audit + repair); version bump b218 -> b233 (+15, capped: several big architecture fixes)

Scope: every user-visible feature traced UI -> event -> state -> core -> backend/tool -> result -> render,
then exercised live in a real terminal (tmux) against LM Studio `gemma-4-e2b-it-qat@q4_k_xl`.

### Files touched:
- `crates/core/src/loop_.rs` — cancel keeps the protocol valid (every tool call answered, partial text kept);
  text tool-call scanner wired in (advertised names only); unique ids for empty/duplicate call ids; nameless calls
  dropped; stall-nudge plumbing removed from returned history.
- `crates/core/src/permissions.rs` — shell rules never match `$(`, backticks, redirections; `narrow_prefix` +
  `PermissionState::allow_always` (per-segment shell rules); MCP read-only hint hook; `spawn_agent` = Read; JSON repair for rule checks.
- `crates/core/src/subagents.rs` — child aborted with the parent call; failures reported as errors; live counter guard.
- `crates/core/src/prompt.rs` — system prompt no longer names non-existent tools.
- `crates/llm/src/openai.rs` — bounded 400 adaptation (was unbounded recursion -> stack overflow); in-stream error
  payloads; idle/connect timeouts; transport retries; `base_url()`.
- `crates/llm/src/parse.rs` — `TextToolScanner::finish()`, raw markup on events, unparseable blocks returned as text,
  object-typed tool arguments.
- `crates/tools/src/shell.rs` — process groups, tree kill on timeout/drop, bounded pipe drain, null stdin.
- `crates/tools/src/patch.rs` — insertion hunks applied (were silently skipped), shared apply/preview.
- `crates/tools/src/mcp/{client,manager,protocol}.rs` — server-initiated requests, empty capabilities, `readOnlyHint`,
  pagination, kill_on_drop, start errors, invalid-args error; bash tests gated to unix (CI was red on Windows).
- `crates/svc/src/updater.rs` — SHA256SUMS verification, packages never installed as binary, newest-by-version
  release selection, `stable` tag, no background beta downgrade, dev-mode cwd check limited to this repo.
- `crates/tui/src/main.rs`, `lib.rs`, `settings.rs`, `autocomplete.rs`, `tips.rs`, `mcp_view.rs`, `wizard.rs` —
  cooperative cancel + turn ids + abort fallback; error lines; parsed/sanitised approval card; narrow Always;
  settings synced with live state and persisting only changed defaults; real tool probe; compaction fix; resume fix;
  context gauge; `/effort` validation and `default`; `/skills`; skill lookup; recap ordinal; ChatView index shift;
  gate lost-wakeup and clear-on-drop; dead settings removed; lying tips fixed; crash log location; memory budget.
- `.github/workflows/release.yml` — SHA256SUMS, raw Windows exe, rolling `beta`/`stable` channel releases, fixed notes.
- `install.sh`, `install.ps1` — prefer the rolling `beta` release.
- Docs: `README.md`, `CHANGELOG.md` (new), `CONTEXT.md`, `ROADMAP.md`, `ARCHITECTURE.md`, `AGENTS.md`, `SECURITY.md`.
- Version: `README.md`, `packaging/arch/PKGBUILD`, `packaging/build-all.sh`, `packaging/bump.sh`, `crates/svc/build.rs`.

### Verification:
1. `cargo test --workspace` — exit 0; 273 passed, 0 failed (baseline 241): core 64, data 5, llm 41, svc 9, tools 52, tui lib 84, tui bin 13, ui 5.
2. `cargo clippy --workspace --all-targets --all-features -- -D warnings` — exit 0 (baseline: 4 errors in test code).
3. `cargo clippy --workspace -- -D warnings` — exit 0.
4. Old-code reproductions (temporary git worktree at dd5365f):
   - shell `sleep 8; echo never` with 200 ms timeout: old code returned after 8.0 s; new code < 1 s.
   - persistent HTTP 400 with `[3]` in body: old code stack-overflowed (SIGABRT); new code returns `Status 400` after <= 3 requests.
   - cancel during a 2-call tool turn: old history had 2 tool calls, 0 results; new history answers both.
5. Live TUI runs (tmux, isolated HOME, LM Studio gemma-4-e2b): list_dir tool turn; Manual-mode approval card showing
   the command and narrow "Always" (`git status`); Esc mid-answer keeps partial text in model context (model quoted
   tip #3); Esc during `sleep 25` settles in 0.4 s, no process left, model sees "cancelled by user"; Settings open in
   live mode and Run Tool Test -> `PASS: native tool call in 3.8s`; F1 breakdown (tools 1636 tok, total matches server
   prompt tokens); Ctrl+R; `/compact` -> single system message with summary, turn kept; `/exit` + `--resume`;
   MCP mock server: discovery, readOnlyHint tool runs without approval in Manual, mutating tool shows args and is
   denied; `/goal` creates and verifies a file and restores the mode.
6. `cargo build -p flashagent-ui --bin b0` + `timeout 4 target/debug/b0` — builds, event loop runs until killed (exit 124).
7. Cyrillic scan over tracked files — no matches.

### Open questions:
- The rolling `beta` release object has to be recreated (delete + create) for b233 so that clients that pick the newest-created
  pre-release (all b218 installs) see it; CI now does that on a `bNNN` tag push. Needs the owner's go-ahead.

### Handoff:
- A10 `/goal` remains partial (budgets, snapshots, milestone commits, live plan, final report).
- `crates/data` is not wired into the TUI; sessions stay JSON until B2.
- MCP tool names that start with read/get/list/... are still auto-classified read-only by name; a server can mislabel a
  mutating tool that way. Consider trusting only `readOnlyHint` / config.
- Esc on an empty prompt quits (documented, unchanged).
