# 2026-09-08 — A7: TUI

### Status: PASS
### Decision: A7 закрыт. TUI — тонкий in-process клиент (svc/IPC — отдельная веха).
### Тестируемое ядро в lib.rs, терминальная обвязка в main.rs (crossterm, Linux).

Files touched:
- crates/tui/Cargo.toml (зависимости: core, llm, tools, crossterm; bin flashagent-tui)
- crates/tui/src/lib.rs (ChatView: события цикла → строки с типами; wrap по ширине;
  approval_card с diff-раскраской; TuiGate — ApprovalGate, отвечаемый клавишей a/d)
- crates/tui/src/main.rs (BackendSource поверх OpenAiCompat; keyboard-поток → канал;
  рендер-цикл; memory-инъекция в первое сообщение; PermissionedTools + WritePreview)
- crates/core/src/loop_.rs (AgentLoop::cancel() — геттер флага)

Verification:
- cargo test --workspace → 77 passed, 0 failed (tui 4)
- cargo clippy --workspace -- -D warnings → Finished, 0 warnings
- Смоук на Linux (headless): бинарник стартует, альтернативный экран, статус-бар,
  подхват памяти (1 doc = AGENTS.md проекта), без --model → осмысленная ошибка.
  Интерактивная клавиатура в headless недоступна — живой прогон за владельцем.

Ключевые свойства:
- Чат: стрим ассистента живой, reasoning серым, тулы ▶/✓/✗, Done-статус
- Подтверждения: TuiGate + карточка с диффом; a = Allow, d = Deny; gated вызовы
  не доходят до executor'а (правило A5 сохранено сквозь весь стек)
- Esc = стоп/выход (cancel-флаг цикла), Ctrl+C = выход
- Память A6 влита в первое сообщение сессии

Open questions: нет. Живой интерактивный прогон — за владельцем (Linux или Windows Terminal).

Handoff: следующая веха — A8 (субагенты: динамические роли, лимит параллелизма,
ревью родителем, каналы, наследование прав без расширения). Читать ROADMAP.md.
