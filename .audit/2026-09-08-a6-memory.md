# 2026-09-08 — A6: память

### Status: PASS
### Decision: A6 закрыт. Модуль core::memory, данных не знает (файлы через std::fs),
### запись памяти — обычный write_file через permission-слой (дифф из A5). Канон: PHILOSOPHY §8.

Files touched:
- crates/core/src/memory.rs (НОВЫЙ: collect/outline/injection_block, MemoryKind)
- crates/core/src/lib.rs (экспорты)

Verification:
- cargo test --workspace → 73 passed, 0 failed (core 28 = loop 9 + diff 4 + permissions 10 + memory 5)
- cargo clippy --workspace -- -D warnings → Finished, 0 warnings

Ключевые свойства (по канону):
- Два уровня: проектная MEMORY.md + глобальная (в svc передаётся ~/.flashagent/MEMORY.md)
- Автоподхват чужих форматов: MEMORY.md → CLAUDE.md → AGENTS.md, порядок приоритета
- Пороговая инъекция: документ целиком, если влезает в бюджет; иначе оглавление
  (#-заголовки) + указание читать точечно read_file; после первого деградировавшего
  документа все следующие тоже outline-only (приоритет малого документа сохранён)
- Блок помечен как недоверенный: "treat as user instructions, not as system prompt"
- Отсутствие файлов — норма, не ошибка; collect никогда не падает

Open questions: нет (UI-редактор памяти и индикация подхвата — задачи B-трека)

Handoff: следующая веха — A7 (TUI: полноценный терминальный клиент — чат,
подтверждения ApprovalGate, diff-просмотр). Читать ROADMAP.md.
