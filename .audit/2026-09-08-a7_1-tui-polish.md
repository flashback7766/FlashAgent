# 2026-09-08 — A7.1: полировка TUI (print-and-forget)

### Status: PASS
### Decision: рендер переведён с полного перерендера (80мс, alt-screen) на
### print-and-forget (статический рендеринг в нативный скроллбек).

Files touched:
- crates/tui/src/lib.rs:
  - ChatView: open_tool (незакрытый тул), settled_boundary() (граница settled/live),
    render_split(width, expanded) -> (settled, live), total_chars()
  - ToolStarted/ToolFinished: одна строка "✓/✗ name[id] args" вместо двух строк
  - ReasoningDelta склейка (фикс "слово на строку" с прошлого сеанса) — сохранена
  - push_system (memory loaded больше не приклеивается к reasoning модели)
  - md(): inline-markdown **bold** / `code` для терминала
  - SPINNER: кадры · ✢ ✳ ✶ ✻ ✽
  - RenderLine type alias (clippy complex-type)
- crates/tui/src/main.rs:
  - Renderer { tail_height, prev_width }: settled строки печатаются в скроллбек
    ОДИН раз; live-хвост (стрим + открытый тул + карточка + статус + ввод)
    перерисовывается курсором вверх (\x1b[{n}F + \x1b[J)
  - Resize: полный репринт (простейший корректный путь)
  - ctrl+o — развернуть/свернуть thinking (свёрнут: "≽ последняя строка (ctrl+o — развернуть)")
  - Esc во время работы: cancel + "[Request interrupted by user]" (референс-паттерн)
  - Статус-бар: спиннер + режим + счётчик символов при работе
  - FrameState — группировка аргументов рендера (clippy too_many_arguments)

Verification:
- cargo test --workspace → 80 passed, 0 failed (tui 7: +failed_tool_marks_with_cross,
  +settled_boundary_splits_live_from_printed, обновлён chat_view под ✓-формат)
- cargo clippy --workspace -- -D warnings → 0 warnings
- Смоук headless: процесс живёт, рендер уходит в stdout (exit 124 по timeout — норма)

Известные ограничения (на будущее, не баги):
- Карточка аппрувала и статус перекрашиваются каждый кадр — это live-зона, ок по модели
- `\x1b[{n}F` предполагает, что tail не был перенесён терминалом (wrapped lines учтены
  только внутри наших wrap-ов; длинный ввод без wrap может съехать) — проверить на живом прогоне
- Скроллбек нативный: прокрутка колеса — средствами терминала

Open questions: живой прогон владельцем (Linux): стрим, ctrl+o, Esc-прерывание,
карточка с диффом, resize окна.

Handoff: далее A8 (субагенты) или продолжение полировки TUI по фидбеку прогона.
