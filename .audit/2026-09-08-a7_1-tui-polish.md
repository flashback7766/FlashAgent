# 2026-09-08 — A7.1: TUI Polish (Print-and-Forget Stabilization)

### Status: PASS
### Decision: A7.1 closed. Terminal renderer stabilized.

Files touched:
- crates/tui/src/lib.rs:
  - ToolStarted/ToolFinished: single line "✓/✗ name[id] args" instead of fragmented rows
  - ReasoningDelta consolidation (fixed "word per line" streaming issue)
  - push_system: memory loaded messages separated cleanly from reasoning
  - md(): inline markdown support (**bold**, `code`) for ANSI terminal
  - SPINNER: animation frames · ✢ ✳ ✶ ✻ ✽
  - RenderLine type alias
- crates/tui/src/main.rs:
  - Renderer { tail_height, prev_width }: settled lines printed to scrollback ONCE; live tail repainted via cursor upward moves (`\x1b[{n}F` + `\x1b[J`)
  - Resize: full clean reprint
  - ctrl+o: expand/collapse reasoning preview
  - Esc during execution: cancel turn + "[Request interrupted by user]"
  - Status bar: spinner + permission mode + character counter during inference
  - FrameState: grouped renderer parameters

Verification:
- `cargo test --workspace` → 80 passed, 0 failed
- `cargo clippy --workspace -- -D warnings` → 0 warnings

Handoff:
- Next: A8 — subagents or further TUI polish per owner feedback.
