# 2026-09-08 — A4: Builtin Tools & Shell Isolation

### Status: PASS
### Decision: A4 (builtin tools + shell isolation in crates/tools)

Files touched:
- Cargo.toml (+glob, tempfile in workspace)
- crates/tools/Cargo.toml, src/lib.rs, src/shell.rs, src/fs_tools.rs, src/web.rs
- crates/core/src/loop_.rs: fixed multi_tool_calls_execute_in_order (sequential execution of parallel tool calls in deterministic order)

Verification:
- `cargo test --workspace` → TOTAL passed: 52, failed: 0 (tools: 13/13, core: 9/9, llm: 20/20, data: 5/5, ui: 5/5)
- `cargo clippy --workspace -- -D warnings` → Finished, 0 warnings

Tools implemented (9 total per PHILOSOPHY):
1. run_shell — foreground / background (via spawn + kill), execution timeout, stdout/stderr capture
2. read_file — line range [offset, limit], char-boundary safety, line numbering option
3. write_file — full file write, creates parent directories
4. edit_file — search-and-replace by chunks (exact match), preserves indentation
5. list_dir — directory listing with sizes and directory indicators
6. glob_find — glob pattern matching, cap at 100 entries
7. grep_search — ripgrep / regex search across files, line numbers, snippet matches
8. fetch_web — HTTP GET, converts HTML to readable text, cap at 50k chars (opt-in only per PHILOSOPHY)
9. duckduckgo_search — HTML scrape (no API keys required), top-N search results (opt-in only)

Design:
- BuiltinTools implements ToolExec trait from core
- Self-healing arguments: repairs python-like single-quoted dicts, unwraps extra nested json objects
- Hostile / invalid arguments returned as tool execution errors, never crashing or panicking the process

Open questions:
- None.

Handoff:
- Next: A5 — permissions (manual/autonomic/planning/bypass, approval gate, diff preview).
