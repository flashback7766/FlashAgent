# 2026-09-08 — A4: тулы + шелл-изоляция

### Status: PASS
### Decision: A4 закрыт (решения владельца: один run_shell с task_id, edit с chunks,
### glob+regex crates, Brave Search API без ключа = тул скрыт, fixed cwd)

Files touched:
- crates/tools/Cargo.toml (зависимости: core, llm, tokio process/fs/time, glob, regex, reqwest)
- crates/tools/src/lib.rs (BuiltinTools: ToolExec, 9 спеков, dispatch, truncate 32k, hostile-args тесты)
- crates/tools/src/fs_tools.rs (read/write/edit-chunks/list_dir/glob/grep, binary-skip, .git|target|node_modules skip)
- crates/tools/src/shell.rs (run_foreground c timeout+kill, ShellRegistry: spawn/status/kill по id, ring-буфер 200k)
- crates/tools/src/web.rs (fetch_text c html_to_text, Brave search)

Verification:
- cargo test --workspace → все ok: 9+5+20+13+5+прочие = 57 passed, 0 failed
- cargo clippy --workspace -- -D warnings → Finished, 0 warnings

Ключевые свойства:
- Хостильные аргументы (мусорный JSON, пустой, неправильные типы, неизвестный тул) → is_error, не паника
- Инъекции: результат тулов — только content в ToolOutput, роль Tool назначает цикл (покрыто тестом A3)
- web_search скрыт из specs() без ключа Brave (8 тулов вместо 9)
- Фоновые задачи: детерминированные id с 1, status/kill через тот же run_shell с task_id

Open questions: нет

Handoff: следующая веха — A5 (разрешения/песочница: правила доступа к путям для тулов,
подтверждения опасных операций). Читать ROADMAP.md.
